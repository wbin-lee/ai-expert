# 260429 오후 강의 요약 — LLM 프롬프팅 · GPT-3부터 Test-Time Scaling까지

> 오후 흐름: **GPT-3 등장과 In-Context Learning → Instruction Tuning(Post-training) → Chat Template → CoT (Chain of Thought) → Program of Thought → Test-Time Scaling (Sequential / Parallel) → LLM Evaluation**
> 강사: 차혜준 교수님 연구실 박사 진학 예정자 (4년차 연구).

---

## 1. LLM의 시작 — GPT-3와 In-Context Learning

### 1.1 ⭐ GPT-3가 LLM이라는 용어를 만든 모델 (시험 출제 가능)
- 그 전엔 "Pre-trained Model", "MLM", "Causal LM"이라 불렀음.
- **GPT-3는 GPT-2와 아키텍처는 동일** (Causal Language Model = Decoder-only).
- 차이점: **파라미터 사이즈가 1.5B → 175B로 폭증**.
  - 용량으로 따지면 약 6GB → 1TB.
- 왜 키웠는가: GPT-2도 텍스트 생성에서 센세이션을 일으켰고("이거 풀면 인류적으로 위험"이라는 OpenAI 트윗으로 조롱당함), **스케일링과 컴퓨팅 파워로 발전한 분야니까 일단 때려박아보자**.

### 1.2 GPT-3는 풀튜닝 모델이 아니다 (중요)
- 풀튜닝 = post-training (instruction tuning + RLHF)이 안 된 상태.
- 즉 **Text Completion 모델** — "안녕 너 누구니?"에 답하는 모델이 아니라, 주어진 텍스트의 **다음 토큰을 예측**.
- 예: "Explain the moon landing to a 6-year-old" → 답이 아니라 그 문장 뒤에 이어질 텍스트(다른 비슷한 형식의 문장)를 생성.

### 1.3 ⭐⭐ In-Context Learning (ICL) — 시험 출제 핵심
- 별도 학습 없이 **Prompt에 예시 몇 개를 넣어주면 그 패턴을 따라하는 능력**.
- 예시 개수에 따라:
  - **Zero-shot**: 예시 0개.
  - **One-shot**: 예시 1개.
  - **Two-shot**: 예시 2개.
  - **3-shot 이상부터는 보통 Few-shot으로 통칭**.

#### ICL의 특성 (시험 냄새 ↑)
- **파라미터 ↑ → ICL 성능 ↑**
- **shot 수 ↑ (zero → one → few) → 성능 ↑**

### 1.4 ⭐ ICL은 "Learning"이 아니다 (개념적 주의)
- 한국어로 "학습"이라 하면 헷갈리지만 **파라미터 업데이트 없음**.
- 면접 시 "ICL했다"를 "학습했다"로 표현하면 → 파라미터 학습으로 오해 → 신뢰도 ↓.
- **올바른 표현**: "In-context learning을 했다" (그대로) 혹은 "프롬프트에 예시를 넣었다".
- 진짜 학습 = "Training" / "파라미터 업데이트".

### 1.5 ICL = Meta-learning ("Learning how to learn")
- 실제로 뭔가 학습한다기보다 **패턴을 인식**.
- 현대 에이전트 시대에도 핵심 — 외부 환경/툴 사용 시 **Experience(이전 시도/실패)**를 컨텍스트에 넣어주면 따라함.
- "Tool 사용법" 같은 in-context learning은 OpenAI Codex, Claude Code에 다 들어있음.

### 1.6 In-Context Learning 실습 (Task 1.1)

#### 사용 모델
- **Qwen2.5 0.5B (Pre-trained)** — text completion 시연.
- **Qwen2.5 0.5B Instruct** — 그 다음 task에 사용.

#### 실습 1: Hello, Who are you
- Pre-trained 모델 → "How long have you been working here? Where are you from?" 같은 **text completion** 결과.
- Temperature 높이고 max_token 150으로 끝까지 생성 → 중국어까지 등장 ("QN(Qwen) 아니랄까봐 중국어").

#### 실습 2: 1+1 = ?
- "Solve the problem 1+1 =" → 그 뒤에 "2" 가 나올 확률 ↑.
- 80 토큰 max라 "2 to find the answer..." 같은 의미 없는 텍스트가 뒤에 붙음.

### 1.7 GSM8K Few-shot CoT 실습 (Task 1.1 본편)

#### 시나리오
- 2023년 ChatGPT 들고 회귀한 상황 → 팀장이 갑자기 "내일 9시 회의 발표 자료 만들어줘".
- 영어 초등 수학 문제(GSM8K) 5문제를 **Pre-trained 0.5B 모델로 Few-shot Prompting**.

#### 실습 포인트
- **Prompt Template + Placeholder + 3개의 Few-shot 예시 + Target 문제**.
- LLM(ChatGPT/Gemini)에 코드 생성 요청 → 직접 코딩 X.

#### ⭐ Indicator의 중요성 (실전 노하우)
- Token max 길게 하면 모델이 **자기 스스로 다음 example 만들고 답까지 생성** → 그 답이 파싱돼버림.
- 해결: prompt에 `Example1`, `Example2` 같은 **identifier**를 명시 → 모델이 "Example5" 같은 다음 indicator 생성하면 거기서 끊기 (`stop_token` 처리).
- → 2년 전 prompt engineering 시대의 핵심 노하우.

> "예전 prompt engineering = LLM 답변이 불안정하니 stable하게 만드는 노가다 작업" — 시대를 거쳐 context engineering, harness engineering으로 변천.

---

## 2. ⭐ Post-Training: Instruction Tuning + RLHF

### 2.1 왜 Post-Training이 필요한가
- Pre-training만으론 **사용자 질문에 답하는 형태**가 아님 → 서비스 부적합.
- Post-training = **Instruction Following** 능력 부여.

### 2.2 Conventional Post-Training의 두 단계 (시험 출제)
1. **Instruction Tuning (Supervised Fine-Tuning, SFT)**
2. **RLHF (Reinforcement Learning from Human Feedback)**

### 2.3 현대 Post-Training (참고)
- **RLVR (RL with Verifiable Reward)**
- **Multimodal Training** (이미지 input 등)

### 2.4 InstructGPT (= GPT-3.5 직전)
- ChatGPT가 나오면서 일반 대중에 충격.
- "Unsupervised learning is the dark matter of AI" (Yann LeCun)는 곧 pre-training이며, **레이블 없이 책 한 권 통째로 먹이는 것**.
- Pre-training만 하면 **말은 하지만 컨트롤 안 되는 괴물**.
- + Instruction Tuning → 어느 정도 사람 모양.
- + **RLHF**가 결정적 — ChatGPT의 대화 능력은 RLHF가 만들어냈음.

### 2.5 Instruction Tuning의 변천사
1. **초기**: 단답형 / answer-only.
2. **Supernatural Instructions**: 1600개 task 모음 — **데이터셋을 누가 더 많이 긁어모으냐**가 경쟁.
3. **데이터 퀄리티 시대**: GPT-4가 markdown으로 답변 잘하도록, 답변을 두세 줄에서 풀어쓰도록 의도적 데이터 가공.
4. **Chat Template 시대** (현재).

### 2.6 ⭐ Chat Template (시험 출제 가능)
- 핵심: **토큰으로 발화자(role)를 구분**.

#### 구조
```
<|im_start|>system
You are a helpful assistant.
<|im_end|>
<|im_start|>user
What is instruction tuning?
<|im_end|>
<|im_start|>assistant
[모델이 답변할 차례]
```

#### 구성 요소
- **System prompt**: 모델 정체성/역할 부여 (ex. "너는 코드 리뷰어야").
- **User prompt**: 사용자 입력.
- **Assistant**: 모델 답변.
- 각 role을 토큰으로 **명확히 구분**.

#### 왜 필요한가
- User는 절대 prompt를 완벽하게 작성하지 않음 (오타, 엔터, 뜻 모호).
- Pre-training만 한 모델은 "어디까지가 user prompt인가"가 애매.
- Chat template로 토큰 단위 명시 → 모델이 정확히 인지.

#### 🔥 학습 시 중요 포인트 (시험 출제 가능)
- **Gradient descent가 assistant 토큰 안의 토큰들에만** 적용됨.
- User/System 토큰은 **마스킹** — 학습 X.
- 이유: User가 욕설("fuck prompting" 같은 2023년 유행어)할 수도 있고, 그것의 next-token 확률을 높이면 안 됨.
- **모델은 자기가 생성해야 할 답변(assistant)만 학습**.

#### Apply Chat Template (HuggingFace)
```python
messages = [
    {"role": "system", "content": "..."},
    {"role": "user", "content": "..."}
]
tokenizer.apply_chat_template(messages, add_generation_prompt=True)
```
- `add_generation_prompt=True` → `<|im_start|>assistant`까지 추가 → 모델에게 "너 생성할 차례야"라고 명시.

#### 토크나이저 chat_template 출력해 보는 습관
- 모델이 무엇을 신경쓰는지 보임 (ex. function calling 토큰, tool calling 형식).
- Qwen2.0엔 function calling 없었지만 2.5엔 있음.

### 2.7 Instruction-Tuned 모델 실습 (Task 1.2)
- Qwen2.5 0.5B Instruct → "hello who are you?" → 정상적으로 답변.
- JSON 형식으로 결과 받기 같은 task에서 **포맷 강제**가 가능.
- **0.5B 정도면 GPT-2와 거의 차이 안 나야 하지만 LLM처럼 잘함** — 이유:
  - GPT-3 시기엔 학습 토큰 빌리언 단위.
  - 이제는 **13 trillion** 토큰 정도까지 학습.
  - **데이터 정제 + 학습량 ↑ → 모델이 미친 듯이 똑똑**.

### 2.8 ⭐ 학교에서의 인사이트 (시험 출제 가능)
> **아키텍처는 capacity를 결정 (얼마나 똑똑해질 수 있냐).**
> **실제 똑똑함은 데이터에 달려있다 — 정제 / 설계 / 양.**

→ 성능 향상의 본질은 **데이터를 어떻게 학습시키는가**.

### 2.9 Zero-shot Instruction-Tuned 모델 비교 실습 (Task 1.2 후반)
- 시나리오: 팀원이 "Few-shot 말고 Instruct 모델로 zero-shot 해봐도 되지 않을까?".
- 5개 샘플로 비교.
- 결과: 작은 모델은 prompt에서 "Final answer만 적어, 박스나 달러 쓰지마"라고 해도 안 들음.
- → **Prompt engineering 한계** → context engineering / harness engineering으로 변천.

---

## 3. ⭐ How to Better Use LLMs — Chain-of-Thought (CoT)

### 3.1 ⭐⭐ CoT 핵심 (시험 출제 핵심)
- **Standard prompting**: 모델이 중간 과정 없이 답만 생성 → 어려운 수학 문제에선 한계.
- **Chain-of-Thought**: 모델이 **중간 추론 과정을 생성**하도록 유도 → 정확도 ↑.
- 모티베이션: 사람도 수학 26번 문제를 암산만으로 풀 수 없음 → 모델도 마찬가지.

### 3.2 Few-shot CoT vs Zero-shot CoT
| 종류 | 방법 |
|------|------|
| **Few-shot CoT** | Prompt에 **중간 추론 포함된 예시** 몇 개 제공 |
| **Zero-shot CoT** | Prompt 끝에 `Let's think step by step` 한 줄 추가 |

> ⭐ **시험 출제**: "이 prompt를 zero-shot CoT로 바꿔라" → 끝에 **"Let's think step by step"** 추가.

### 3.3 CoT 효과
- GSM8K 같은 벤치마크에서 **18% → 57%** 정도 점프.
- 다양한 reasoning task에서 큰 향상.

### 3.4 ⭐ CoT 시대의 파급효과 (시험 출제 가능)
1. **연구가 심리학과 비슷해짐**: "소크라테스처럼 생각해봐" 같은 prompting 논문 쏟아짐.
2. **2023년 = Prompting Era**: 문과 출신이 AI 석사로 대거 진입 → 프롬프팅 논문 많이 씀.
3. **CoT Distillation**: 큰 모델로 CoT 데이터 만들어 → 작은 모델에 학습 → 작은 모델 성능 ↑.

### 3.5 ⭐ Token Information Density (Jason Wei 강조)
- 토큰마다 "정보 밀도"가 다르다.
- 예: "Large language ___" → "model"이 63% 확률.
- 반면 "the answer is ___" 같은 곳은 **a, b, c, n** 같은 토큰들이 비슷한 확률.
- **CoT는 어려운 부분에서 추가 토큰을 소비해 확률을 sharper하게 만듦**.

### 3.6 핵심 메시지
> **"몇몇 문제는 다른 문제보다 더 많은 계산이 필요하다."**
> **"토큰을 많이 소비할수록 끝의 확률이 높아진다."**

### 3.7 CoT 모델은 이제 기본
- 0.5B 모델조차 자동으로 CoT 함.
- CoT 없는 데이터는 학습 자체를 안 시킴 → 기본 behavior.
- → 강사가 실습을 다시 만든 이유 (CoT 없는 모델 찾기 불가능).

### 3.8 Few-shot CoT 실습 (Task 2.1)
- API: Groq (무료) — `https://groq.com/`에서 회원가입 → API key 생성.
- Framework: **LangChain** — LLM 활용 개발의 사실상 표준.
- 모델: Llama 3.1 8B 같은 작은 모델 with naive few-shot CoT.

#### 실전 Tip — CoT Quality
- **상위 모델(GPT-5.5 같은)에게 문제를 풀게 하고**, 중간 reasoning이 좋다 싶으면 → 그걸 **few-shot prompt에 복붙**.
- 작은 모델이 그 reasoning을 따라하도록 강제 → 성능 ↑.

---

## 4. ⭐ Program of Thought (PoT)

### 4.1 PoT의 모티베이션
- LLM은 **수학적 산술이 약하다** (논리적 추론은 잘하지만).
- 이유: **토크나이즈 방식** 때문.
  - "1723"은 인간에겐 통째 숫자지만, 토크나이저는 "17" + "23"으로 쪼갬.
  - "+ 7" 같은 띄어쓰기 변화로 같은 숫자가 다른 토큰이 됨.
  - → 같은 7도 위치/공간에 따라 다른 토큰.
  - → 인간이 받아들이는 숫자와 모델이 받아들이는 숫자가 다름.

### 4.2 ⭐ PoT 아이디어 (시험 출제 가능)
- LLM이 **코드는 잘 짜니까** → 문제를 코드로 해결시키고 **Python interpreter로 실행**.
- LLM 출력 = **코드** (수학적 답이 아님).
- 코드를 추출해 실행 → 결과 값.
- 예: 피보나치 50번째 → 모델이 직접 계산 어려움 → 코드는 쉽게 작성 가능.

### 4.3 PoT 실습 (Task 2.2)
- 시나리오: 강사 형이 PWC(컨설팅) 매니저였을 때 인턴에게 "PoT라는 거 우리 데이터에도 해봐" → 실제 발언.
- 20문제 GSM8K에 대해 PoT.
- Prompt → 코드 생성 → Python extractor → 코드 실행 함수 → 결과 저장.

---

## 5. ⭐⭐ Test-Time Scaling (테스트 타임 스케일링)

### 5.1 ⭐ 정의 (시험 출제 핵심 — 최신 키워드)
- **Inference 시(Test-Time) 컴퓨팅 자원을 더 쏟아 성능을 높이는 모든 접근**.
- CoT가 잘 되는 이유 = "토큰 많이 소비하면 정확도 ↑" → 이를 확장한 패러다임.

### 5.2 ⭐ 두 가지 Type (시험 출제 가능)
| 타입 | 컨셉 | 예시 |
|------|------|------|
| **Sequential** | 순차적으로 컴퓨터를 더 때려박음 | Thinking Mode, Self-Refinement |
| **Parallel** | 여러 개 동시 생성 후 선택 | Majority Voting (Self-Consistency), Best-of-N |

### 5.3 ⭐⭐ Thinking Mode (Sequential) — 시험 출제 핵심
- **GPT-5 thinking, Gemini 심층 생각, DeepSeek R1, o1, o3** 등.
- 핵심: **CoT reasoning chain을 미친 듯이 길게 생성**.
- Generation token 만 단위 (1만 토큰).
- DeepSeek R1이 처음 떠서 주식 폭락 시킨 것도 이 thinking mode.
- 학습 측면: **RLVR**로 학습.

#### Thinking Token
- Claude 등에서 "더보기"로 숨겨진 reasoning trace.
- `<think>` 같은 토큰 안에 Long CoT 생성.
- Thinking 내용은 사용자에게 직접 안 보여주는 경우 多 (relevant 정보가 아니라서 toggle 처리).

#### 어떻게 얻나
- **Long CoT Distillation** + **RLVR Training**.

#### 활용
- Thinking 모드 base model을 가져와 프로젝트에 사용.
- Thinking token 안에 "이렇게 분석해", "이때까지 작성한 거 한 번 더 검토해"라고 명시 → 더 길게 사고 + 성능 ↑.
- DeepSeek R1 논문에 **"아하 모먼트"** — RLVR 학습 중 모델이 스스로 잘못 깨닫는 현상.

### 5.4 ⭐ Self-Refinement / Self-Reflection (Sequential) — 시험 출제 가능
- 2023년 논문 (`Self-Refine`).
- 미친 듯이 단순한 아이디어:
  ```
  Input → Model → Output
                ↓
              Feedback (같은 모델이 자기 output 평가)
                ↓
            Refined Output (같은 모델이 feedback 받아 수정)
  ```

#### 왜 같은 모델로 가능한가
- 모델은 task별로 능력치가 다름:
  - **Initial Generation Task**
  - **Critique Task** (틀린 것 찾기)
  - **Refinement Task** (피드백 받아 수정)
- 각 task가 별개라서 self-loop이 의미 있음.

#### 현대 에이전트 시대의 핵심 컨셉
- Claude Code / Cursor가 에러 메시지 받고 스스로 수정 → 같은 원리.
- Multi-agent라고 해도 **기반 모델은 동일** — system prompt만 다르게 줘서 역할 구분.

#### 실습 (Task 2.3)
- GSM8K에서 틀린 문제만 → Critic이 피드백 → Refiner가 다시 풀이.
- Initial → Critique → Revision prompt를 확장하면 코드 리뷰어, 멀티 에이전트 등으로 발전 가능.

### 5.5 ⭐ Majority Voting / Self-Consistency (Parallel) — 시험 출제 가능
- 단순한 아이디어: **CoT를 여러 번 생성 → 답이 가장 많이 나온 것을 선택**.
- 예: 18, 18, 20, 18, 22 → 18이 majority.
- 원논문: 2022 "Self-Consistency Improves CoT Reasoning" (4천 회 인용).
- 그 시대엔 **CoT-SC**로 불렸지만, 지금은 **Test-Time Scaling - Parallel** 카테고리에 속함.
- → **용어가 변하지만 본질은 비슷**.

### 5.6 ⭐ Best-of-N (BoN) — 시험 출제 가능
- 여러 개 생성 후 **Reward Model로 점수 매겨 가장 높은 것 선택**.
- Reward Model의 품질이 결과를 좌우.

#### Reward Model이란
- **Language Model이 base** — 마지막 generation 부분에 **scalar 값** 출력하도록 학습.
- 다음 토큰 확률 대신 **scalar reward** 출력.
- Prompt + Answer → Scalar Score.
- 학습: 인간 선호도(좋으면 1, 나쁘면 0)로 학습 → RLHF의 핵심 컴포넌트.

#### 왜 Language Model로 만드나
- 수학처럼 답이 명확하지 않은 task ("글 써줘", "여행 계획", "오늘 뭐 먹을까")도 평가 가능해야 함.
- LLM의 language understanding을 활용해 인간 선호 담을 수 있음.

### 5.7 MCTS, Tree-of-Thought (확장)
- **MCTS (Monte-Carlo Tree Search)**: 트리 서치로 verifier(reward) 활용해 가장 높은 답 선택.
- **Tree-of-Thought**: CoT와 Test-Time Scaling 가운데 위치한 컨셉. SC와 비슷 (여러 개 생성).
- 둘 다 현대에는 Test-Time Scaling 우산 아래로 통합 ("아웃데이트된 용어").
- 에이전트 시대에 **여러 step 인퍼런스에서 쓸모없는 것들을 걷어내기** 위해 활용.

### 5.8 ⭐ Test-Time Scaling = Agent와의 연결 (시험 출제 가능)
- Refinement도 결국 **컴퓨터(Python interpreter / API)와 인터랙션**해서 정답 검증.
- → "환경과의 인터랙션" → **Agent의 본질**.
- Test-Time Scaling은 **Agent 시대의 핵심 컴퓨팅 패러다임**.

---

## 6. LLM Evaluation

### 6.1 ⭐ 강사의 경험 기반 인사이트 (시험 출제 가능)
> **예전: "사람 평가는 비싸고 정확, 모델 평가는 저렴하지만 부정확"**
> **현재 (강사 의견): "모델이 더 정확하고 더 비싸다"**

이유:
- 사람(annotator)은 5개 넘어가면 멍 때림 → **일관성 ↓**.
- 모델은 **일관적**.
- 단, 토큰 비용 ↑ → 비용 측면에선 더 비쌈.
- 연구실 한 학회 데드라인 직전 1주에 OpenAI 비용 400만원.

### 6.2 ⭐ Reward Model 대신 LLM-as-a-Judge (최신 트렌드)
- 최근 핫한 연구: **Criteria Prompting** — LLM에게 직접 점수 매기게 함.
- "Correctness, Caringness, Accuracy" 등 항목별 점수 → 평균 → **이게 곧 reward**.
- 별도 reward model 학습 X.
- 단점: prompt engineering으로 충분히 안정적인 평가 능력 있는 모델인지는 검증 필요.

### 6.3 평가 vs 풀이의 분리
- 문제를 푸는 능력 ≠ 평가하는 능력.
- 그러나 **Reward model을 LLM-as-Judge로 퉁치면 self-refinement와 유사한 영역으로 통합**.
- 강사 의견: 메소드(method)보단 **어떤 문제를 푸는가**가 중요.

### 6.4 평가에서 정량화의 용기
> "소프트웨어 엔지니어링은 가장 quantify하기 어려운 것을 quantify해야 하는 용기가 필요하다."

- LLM에게 평가 시키더라도 **무엇을 평가할 것인가**의 기준점 설계가 중요.
- 그 기준을 사람한테 시켰을 때도 정확하게 결과를 낼 수 있는가가 좋은 평가 기준.

---

## 7. 실습 방식 메타 코멘트

### 7.1 강사의 의도
- 2년 전: 코드 한 줄씩 다 설명 → 절반은 ChatGPT 돌려서 흡수, 절반은 멍 때림.
- 이번: **빈칸으로 두고 학생이 LLM에게 prompt해 코드 생성**.
- 실습 = **Prompt Engineering with ChatGPT 동시 활용**.
- 실제 개발 환경(왼쪽 코드 / 오른쪽 LLM 채팅창)을 시뮬레이션.

### 7.2 사용한 도구들
- **Qwen2.5 0.5B + Instruct** — 로컬 GPU.
- **Groq API** (무료, 작은 모델 한정) — Llama 3.1 8B.
- **LangChain** — LLM 통합 framework.
- 강사 선호: **OpenRouter** — 다양한 모델 무료 옵션 (단, 20 콜 후 터짐).

### 7.3 본인 연구실 비용 감각
- OpenAI Organization Tier 최상위.
- 학회 데드라인 1주: 4팀 × 약 400만원.
- Rate Limit 무관하게 token 소비 가능.

---

## 8. 한 페이지 요약

### 8.1 ⭐ 시험 핵심 포인트 (오후)

| 주제 | 핵심 |
|------|------|
| **GPT-3** | GPT-2와 아키텍처 동일(Causal LM), **1.5B → 175B** 파라미터 폭증, LLM 용어의 시작 |
| **GPT-3 ≠ ChatGPT** | Pre-trained = Text Completion, 질문에 답 못함 |
| **In-Context Learning (ICL)** | Prompt에 예시 제공, **파라미터 업데이트 X**, "Learning"이지만 학습 X |
| **Shot 종류** | Zero / One / Few(3+ ) shot |
| **ICL 특성** | 파라미터 ↑ + shot 수 ↑ → 성능 ↑ |
| **Post-Training 2단계** | Instruction Tuning (SFT) → RLHF |
| **InstructGPT = ChatGPT 직전** | RLHF가 대화 능력의 핵심 |
| **Chat Template** | system / user / assistant 토큰으로 role 구분 |
| **Chat Template 학습** | **Assistant 토큰 안의 토큰만 gradient 적용** (나머지 마스킹) |
| **add_generation_prompt=True** | `<|im_start|>assistant`까지 포함시켜 모델에게 생성 차례임을 알림 |
| **Indicator** | Few-shot에서 "Example1", "Example2"로 끊어 → stop token 활용 |
| **인사이트** | 아키텍처 = capacity, **데이터 = 실제 똑똑함** |
| **CoT** | 중간 추론 과정을 생성하게 하기 |
| **Zero-shot CoT** | "Let's think step by step" 한 줄 추가 |
| **Few-shot CoT** | reasoning 포함된 예시 prompt에 |
| **CoT Distillation** | 큰 모델 CoT 데이터로 작은 모델 학습 |
| **Token Information Density** | 토큰마다 정보 밀도 다름, CoT로 더 많은 토큰 소비해 확률 sharper |
| **PoT (Program of Thought)** | LLM이 코드 생성 → Python interpreter로 실행 |
| **PoT 모티베이션** | 토큰화 때문에 LLM은 산술 약함 |
| **Test-Time Scaling** | Inference 시 컴퓨팅 더 써서 성능 ↑ |
| **TTS - Sequential** | Thinking Mode, Self-Refinement |
| **TTS - Parallel** | Majority Voting, Best-of-N |
| **Thinking Mode** | Long CoT (만 토큰), RLVR로 학습 |
| **Self-Refinement** | 같은 모델이 generate → critique → refine, 모델 능력은 task별로 다름 |
| **Self-Consistency / Majority Voting** | CoT 여러 번 → 다수결, 원래 2022 CoT-SC |
| **Best-of-N** | Reward Model로 점수 매겨 best 선택 |
| **Reward Model** | Language Model 기반, 마지막에 scalar 출력 |
| **MCTS / ToT** | 모두 Test-Time Scaling 우산 아래 |
| **LLM-as-a-Judge** | Reward model 대신 LLM이 직접 점수 매김 |
| **현대 평가 (강사 견해)** | 모델이 사람보다 정확하고 비쌈 (token cost ↑) |

### 8.2 LLM 발전 한눈에
```
GPT-2 (1.5B)  ─→  GPT-3 (175B, In-Context Learning)
                  ─→ Instruction Tuning (SFT)
                     ─→ InstructGPT
                         ─→ + RLHF = ChatGPT (대화)
                             ─→ + CoT (Chain of Thought, 2022)
                                 ─→ + Test-Time Scaling
                                     - Sequential (Thinking, Refinement)
                                     - Parallel (Majority Voting, BoN)
                                     ─→ Agent 시대
```

### 8.3 Prompting 패러다임 변천
```
2020: Pre-training만으로 In-Context Learning
2022: CoT (Chain of Thought) — "Let's think step by step"
2023: Prompting Era — 문과 출신 AI 박사 다수, 소크라테스 프롬팅 등
2024: Context Engineering / Harness Engineering
2025+: Agent + Test-Time Scaling
```

### 8.4 Chat Template 도식
```
<|im_start|>system
You are a helpful assistant.
<|im_end|>
<|im_start|>user
What is instruction tuning?
<|im_end|>
<|im_start|>assistant   ← 여기서부터만 학습 (gradient 적용)
Instruction tuning is...
<|im_end|>
```

### 8.5 Test-Time Scaling 도식
```
[Sequential]
  - Thinking Mode (Long CoT, 만 토큰)
  - Self-Refinement (Generate → Critique → Refine)

[Parallel]
  - Majority Voting / Self-Consistency (CoT 여러 개 → 다수결)
  - Best-of-N (Reward Model로 best 선택)
  - MCTS, Tree-of-Thought (트리 서치 + verifier)
```

### 8.6 비유 / 직관 모음

| 개념 | 비유 / 직관 |
|------|-----------|
| **GPT-3 = Text Completion** | "안녕하세요" 받으면 "처음 뵙겠습니다"가 아니라 비슷한 다른 인사문이 줄줄 나옴 |
| **In-Context Learning ≠ Learning** | "공부했다"가 아니라 "예시 봤다" — 면접에서 헷갈리면 신뢰도 ↓ |
| **무한도전** | "역시 모든 게 있는 무한도전이죠" — 강사 표현, ICL 예시가 다양한 task에 다 통한다는 것 |
| **CoT의 모티베이션** | "수학 26번 4점짜리를 암산으로 풀어봐" → 사람도 못 함 → 모델도 마찬가지 |
| **PoT의 모티베이션** | "1723"이 사람에겐 한 숫자지만 모델에겐 "17"+"23"으로 쪼개진 토큰 → 산술 약함 |
| **Thinking Mode** | DeepSeek R1이 처음 떴을 때 주식 폭락 — 작은 모델이 미친 듯 잘하더라 |
| **아하 모먼트** | RLVR 학습 중 "아 이거 잘못됐다"라고 모델이 스스로 깨닫는 현상 |
| **Self-Refinement = Multi-Agent** | "기반 모델은 동일, system prompt만 다르게" — 결국 같은 모델의 다른 역할 |
| **CoT-SC vs TTS-Parallel** | "용어는 변하지만 본질은 같다" — 환경이 바뀌니 새 용어가 추가될 뿐 |
| **Reward Model** | LM의 마지막 generation을 scalar 값으로 바꾼 것 — "다음 토큰 확률 → 점수" |
| **Annotator의 한계** | 사람은 5개 넘어가면 멍 때려서 일관성 ↓, 모델이 일관성 ↑ |
| **소프트웨어 엔지니어링의 용기** | "quantify하기 가장 어려운 것을 quantify하는 용기" |
| **데이터의 가치** | "아키텍처는 capacity 결정, 실제 능력은 데이터에 달려있다" |
| **Prompt Engineering = 노가다** | "LLM 답변을 stable하게 만드는 노가다" — 그게 인사이트로 쌓인 것 |

### 8.7 강사 어록
- "ICL을 한국어 '학습'으로 표현하면 면접에서 신뢰도 떨어진다."
- "Fuck prompting하면 성능 올라가던 시절이 있었다."
- "박사를 가고 싶어하는 인턴들이 (학습 데이터를) 만든다."
- "메소드보다 어떤 문제를 푸는가가 더 중요하다."
- "용어가 바뀌어도 컨셉은 비슷하다 — Tree-of-Thought도 Test-Time Scaling으로 포지션하는 게 받아들이기 좋다."
- "최근 학회에서 발표되는 건 이미 6개월 전 사이클의 결과물이다."

### 8.8 실습 핵심 흐름 정리

| Task | 내용 | 모델 | 핵심 포인트 |
|------|------|------|-------------|
| 1.1 | Text Completion 시연 | Qwen 0.5B Pre-trained | 답이 아니라 다음 텍스트 생성 |
| 1.1 (본편) | GSM8K Few-shot CoT | Qwen 0.5B Pre-trained | Indicator로 stop token |
| 1.2 | Chat Template 출력 | Qwen 0.5B Instruct | system/user/assistant 토큰 구분 |
| 1.2 (본편) | Zero-shot Instruct | Qwen 0.5B Instruct | Pre-trained vs Instruct 비교 |
| 2.1 | Few-shot CoT API | Llama 3.1 8B (Groq) | LangChain + few-shot CoT |
| 2.2 | Program of Thought | Llama 3.1 8B (Groq) | 코드 생성 → Python 실행 |
| 2.3 | Self-Refinement | Llama 3.1 8B (Groq) | Generate → Critique → Refine |
| (3.x) | Majority Voting | Llama 3.1 8B (Groq) | 여러 개 생성 → 다수결 |
