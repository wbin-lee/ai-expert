안녕하세요.
오늘 4일차 오전 실습 시간을 시작해보도록 하겠습니다.
오늘 해볼 것은 트랜스포머에 대해서 알아보고
코드 실습까지 진행을 하도록 할 거고
트랜스포머는 처음에는 자연어 처리 분야에서 시작이 됐지만
음성이나 이미지, 멀티 모델까지 지금 굉장히 넓게 사용되고 있는 아키텍쳐 중에 하나라서
실습 시간을 다뤄보면 좋을 것 같다고 생각을 했고
그리고 여러분 채찌 PT 다들 사용하시죠?
채찌 PT에 기반이 되는 아키텍쳐도 트랜스포머이기 때문에
트랜스포머에 대해서 알아보도록 하겠습니다.
지난 시간에 RNN, CNN 이런 실습들 하셨는데
RNN이 대표적인 sequence to sequence 모델 중에 하나예요.
그런데 여기서 attention이라는 메커니즘이 등장하게 되고
그 attention이라는 메커니즘을 만을 활용한 모델이 트랜스포머 구조입니다.
그리고 트랜스포머 아키텍쳐를 활용한 모델들이 버트나 GPT 등등이 있어서
이 흐름으로 실습을 진행하도록 하겠습니다.
먼저 트랜스포머가 왜 나왔는지 좀 이해를 해보려면
기존의 sequence to sequence 모델을 조금 다시 짚고 넘어가도록 할게요.
sequence to sequence는 줄여서 sick to sick이라고도 하는데
sequence를 입력받아서 sequence를 뱉는, sequence를 생성하는 모델이라고 볼 수 있습니다.
그래서 이런 모델을 활용하는 대표적인 사례가 지금 예시에 나와 있는 것처럼
이런 기계 번역들을 sequence to sequence 모델로 많이 했었는데
예를 들어서 이런 프랑스어로 된 문장이 들어갔을 때
영어로 된 문장으로 번역을 해주는 이런 테스크를
sequence to sequence 모델로 해결을 했었습니다.
그래서 이런 구조는 크게 지금 두 가지로 나눠져 있는데
첫 번째가 입력 문장을 읽어오는 인코더가 되고
두 번째가 그 정보를 바탕으로 출력 문장을 생성하는
decoder 부분이 있습니다.
그래서 이렇게 입력을 이해하고
그리고 그걸 바탕으로 출력을 만드는 부분이
빨간색과 파란색 네모로 분리되어 있다고 보시면 됩니다.
그래서 이런 sequence to sequence 모델이 주로 RNN을 기반으로 했었는데
RNN 기억나시죠? 어제 했던 거
sequence 구조를 하나씩 차례대로 읽어나가는 구조입니다.
그래서 기존의 인코더, 디코더 구조의 RNN에서는
인코더가 문장을 입력 끝까지 읽은 다음에
그걸 하나의 고정벡터로 압축을 시키고
그걸 바탕으로 디코더에 전달을 해서
디코더에서도 단어 하나하나씩 예측을 하는 그런 구조였죠.
그래서 RNN의 문제점이라고 한다면
앞단의 빨간색 부분에서 입력 문장을 요약을 해야 되는데
이게 길어지면 길어질수록
하나의 요약베터에 담는다는 게
정보 손실에 영향을 많이 미친다는 점이 RNN의 단점이었습니다.
만약에 내가 긴 문장을 번역을 하고 싶다고 했을 때
첫 번째 이쪽을 생성할 때는
문장을 정보가 그렇게 희미해지지 않은 상태일 수 있지만
뒤로 갈수록 점점 정보가 희미해지면서
긴 문장을 완성해내기 어렵다는 단점이 있죠.
그거를 information bottleneck, 병목 문제라고 부르고 있고
이제 이거를 해결하기 위해서 증장한 게
attention이라는 방법입니다.
그래서 attention, 단어의 뜻에서 알 수 있듯이
이 방법이 되게 직관적인데
기존 방식을 살펴보면
디코더가 출력 단어를 생성을 해줄 때
인코더의 마지막 요약베터를 참고를 한다고 말씀을 드렸죠.
그래서 이 빨간색 블럭들, 이 인코더 블럭들이 RNN의 각 스텝들입니다.
그래서 단어를 읽을 때마다 이렇게 hidden state가 하나씩 만들어지게 되고
오른쪽에 있는 저 초록색 블럭이 디코더의 현재 상태가 됩니다.
지금은 start라는 토큰을 넣고
첫 번째 출력 단어를 생성하려고 하는 순간이라고 볼 수 있습니다.
이게 기존의 방법이었다면
attention을 사용할 때는 이 인코더가
더 이상 인코더의 마지막 그 state만 보는 게 아니라
왼쪽에 있는 모든 인코더의 hidden state랑 각각 연결을 하게 되고
그렇게 됨으로써 입력 문장 중에
내가 어디를 가장 주의 깊게 봐야 하지를 나타낼 수 있는 게
attention 메커니즘입니다.
그래서 attention을 한 단계씩 살펴보자면
먼저 일단 디코더의 hidden state, 오른쪽에 초록색 벡터 있죠.
이게 지금 막 start를 입력받은 상태고
내가 이 빨간색 밑에 있는 프랑스어 문장을 입력을 받았을 때
어떤 단어를 첫 번째로 뱉어야 될지를 결정해야 되는 상태입니다.
이때 attention이 적용이 되면
이 입력 문장 중에 내가 지금 어떤 부분을 가장 참고해야 되지를
결정을 하는 게 attention 계산인데
그러기 위해서 디코더의 첫 번째 state,
현재 state랑 인코더의 첫 번째 hidden state를 비교를 해줍니다.
비교를 하기 위해서 dot product를 해주게 되고요.
dot product는 두 벡터가 얼마나 비슷한 방향을 나타내는지를
볼 수 있는 방법입니까?
dot product를 사용하고 있고
그래서 첫 번째 입력 단어 쪽에 hidden state가
현재 디코더 상태랑 얼마나 관련이 있는지를
dot product를 통해서 점수로 계산을 해줍니다.
그래서 저렇게 파란색 dot이 하나 생겼죠.
이렇게 첫 번째 hidden state랑 비교를 했으면
그 다음으로는 같은 방식으로
두 번째 인코더 hidden state와도 비교를 해줍니다.
그래서 첫 번째 단어만 보는 게 아니라
두 번째 단어 위치에서도 현재 출력하려고 하는데
얼마나 영향을 끼칠지를 점수를 매겨주는 거죠.
그래서 위에 파란색 점이 하나 더 생기고
이게 두 번째 위치에 대한 attention score라고 보시면 됩니다.
그 다음에 이제 세 번째 hidden state와도
동일한 방식으로 비교를 해줘요.
그래서 지금이 쭉 흘러오는 것의 핵심은
디코더가 인코더의 모든 스텝의 관련도를
하나씩 계산을 하고 있다는 게 포인트가 되고
이 말은 입력 문장 전체를 다 보고
지금 내가 필요한 정보가 뭔지를
score로 계산을 해서 찾는 과정이라고 볼 수가 있습니다.
마지막으로 이제 네 번째 hidden state까지 비교를 했으면
인코더 위에 모든 스텝들의 점수가 매겨져 있죠.
그래서 위에 있는 파란색 점 4개가
입력의 각 위치에 대한 중요도 점수라고 볼 수 있습니다.
그래서 우리는 이거를 attention score라고 부르게 됩니다.
이제 이렇게 구한 attention score들을
실제로 사용을 하는지를 살펴봐야 하는데
이제 아래 파란 점들이 아까 인코더 hidden state에서
계산된 attention score들이죠.
그래서 현재 디코더가 첫 번째 단어
지금 정답은 he라는 단어인데
이 he라는 단어를 생성하려고 할 때
입력에서 각 단어가 얼마나 중요한지를
이 파란색 점으로 나타내놨습니다.
그런데 이 점수들을 그대로 사용하는 게 아니라
softmax라는 것을 활용을 해서 정규화를 시켜주게 돼요.
그러면 위에 보이는 이 그래프들처럼
약간 확률처럼 표현이 됩니다. 확률분포처럼.
그래서 이 값들은 softmax를 태우면
이 합이 1이 되는 구조이기 때문에
우리가 확률처럼 해석을 해줄 수가 있고
지금 그림에서 보면 첫 번째 score가
가장 높다는 것을 알 수가 있습니다.
이 말은 디코더가 현재 출력 단어를 만들 때
이 첫 번째 입력 단어 쪽의 정보를
가장 많이 참고를 하면 되겠다라는
의미를 담고 있습니다.
그래서 이렇게 attention distribution을 만들었으면
이거를 가중치로 사용을 해서
인코더의 hidden state들을 weighted sum을 해줍니다.
그래서 인코더의 hidden state를 weighted sum을 해서
하나의 context vector로 만들어내게 됩니다.
중요한 hidden state는 좀 많이 반영하고
덜 중요한 hidden state는 적게 반영해서
context vector를 만들어주게 되고
그리고 나서 이제 이 decoder가
그 context vector랑 자신의 현재 hidden state를
같이 사용을 해서 출력 단어를 예측을 하게 됩니다.
그래서 he라는 단어가 예측이 된 겁니다.
그래서 이렇게 attention을 사용을 하면
입력을 보면서 지금 가장 필요한 정보에
더 높은 가중치를 둬서
그때그때 좀 더 앞에 있는 문장을 잘 참고해서
단어를 생성해낼 수 있게 만드는 방법이라고 보시면 됩니다.
지금까지는 이 start 토큰을 넣고
첫 번째 단어 he라는 단어를 출력하는 상황을 봤는데
이제 문장을 번역을 하려면
이 한 글자 he로만 끝나는 게 아니라
뒤에도 쭉 번역을 해줘야겠죠.
그래서 방금 생성했던 이 he를
다시 decoder의 입력으로 넣어서
다시 decoder의 입력으로 넣어서
다시 decoder의 입력으로 넣어서
생성했던 이 he를 다시 decoder의 입력으로 넣어서
두 번째 출력 단어를 만들어줘야 합니다.
이때도 똑같이 attention을 다시 계산을 하게 되고
왜 다시 계산하냐고 하면
출력마다 필요한 정보들이 달라지겠죠.
첫 번째 출력에서는 첫 번째 단어가 제일 중요했을 수도 있고
두 번째 출력에서는 세 번째 네 번째 단어가
제일 중요할 수도 있고
그렇기 때문에 attention은 한 번 계산해놓고
계속 쓰는 게 아니라
새로운 단어를 만들어낼 때마다
매 스텝마다 계속 다시 계산해서
사용하는 거라고 이해하시면 되겠습니다.
그러면 첫 번째 단어를 만들었을 때
생성했던 이 attention distribution하고는
또 다른 distribution이 만들어지겠죠.
그렇게 쭉 하는 attention에 사용한
sequence to sequence 전체 과정을
한 장에 모아준 그림이 이렇게 되고
아래 초록색 이 부분이 encoder가 되고
입력 문장을 읽으면서 각 위치마다
hidden state를 만들어내게 됩니다.
그래서 hidden state가 여기 보이는 sk가 되고
그리고 아래 오른쪽 이 부분이 decoder에 해당하고요.
이거는 이제 이전까지 생성한 출력들을 바탕으로
내가 현재 시점에 hidden state인 ht
이거를 만들어내게 됩니다.
그래서 decoder 스텝 t 시점
이 t 시점에서 보자면
여기서 다음 단어를 예측하려고 할 때
또 attention이 새로 동작을 하게 되겠죠.
그래서 왼쪽에 있는 박스에서 볼 수 있듯이
decoder state ht랑 encoder의 각 hidden state sk를
비교를 해서 스코어를 만들어주게 됩니다.
그래서 decoder cap과 encoder hidden state를
계산을 해서 attention 스코어를
계산을 해주게 됩니다.
그래서 이 슬라이드에서는 지금
bilinear를 사용해서 점수를 구한다
라고 되어 있는데 bilinear라는 거는
두 벡터의 관련도를 중간에 이 행렬 w를
사용해서 계산하는 방법이다 라고
생각을 해주시면 되고 아까
앞전 슬라이드에서는 가장 단순하게
두 벡터를 내적해서 유사도를 구했었는데
여기서는 이 학습이 가능한 가중치 행렬인 w를
사이에 넣어서 두 벡터를 비교하는 방식입니다.
그래서 좀 더 좋은 그런
스코어를 만들기 위한 스킬이라고 생각을 해주시면 되겠고
그 다음에는 이 점수들을 softmax에 넣어서
정규화를 해줘야겠죠.
그래서 아까 distribution 만들었던 것처럼
이렇게 softmax를 태워서
합이 1인 distribution으로 만들어줍니다.
이 가중치들을 가지고
encoder hidden state를 weighted sum 해줘요.
그래서 이렇게 weighted sum을 해서
각 위치 위쪽에 있는 초록색 벡터가 생성이 되게 되고
이게 context 벡터가 됩니다.
이거는 decoder step에서 필요한 입력 문맥
입력 context 정보를 요약한 가중치를 줘서
요약한 벡터라고 보시면 됩니다.
그리고 오른쪽에서는 이 context 벡터랑
지금 decoder hidden state를 사용을 해서
새로운 표현인 ht hat을 만들어주게 되고
이걸 통해서 현재 단어의 확률을 계산하고
다음 출력 단어를 예측을 해주게 됩니다.
그래서 이 슬라이드 여기에서는
attention이 실제로 뭘 배우게 됐는지를
히트 맵으로 표현을 한 건데
가로축이 입력 문장 단어들이 되고
가로축이 출력 문장 단어들이 됩니다.
그리고 지금 히트 맵 보면
검정색에서부터 흰색까지
밝기가 다르게 나타나고 있는데
밝기가 높을수록 밝을수록
특정 출력 단어를 생성을 할 때
입력 단어를 얼마나 강하게
반영을 했는지를 나타내 주게 됩니다.
밝을수록 attention weight가 크다라고
말해주시면 되는데
이렇게 보면 attention이 입력하고 출력 사이의
대응 관계를 잘 학습을 하고 있다는 것을 볼 수가 있어요.
전체적으로 밝은 부분이 대각선 비슷하게 나타나는데
이게 번역하는 테스크이다 보니까
입력 문장의 진행 순서에 따라서
출력 문장의 한 단어씩을 생성할 때
비슷한 그 시퀀스로 attention을
넣어주면 좋겠죠.
근데 또 완벽한 대각선 아닌 것을 보면
언어마다 문장의 어순들이 조금 다르기 때문에
완전 직선은 아니고 중간중간
이렇게 휘어지거나 퍼진 부분도 있다고
이해하시면 될 것 같습니다.
그래서 attention의 중요한 역할은
출력 단어를 만들 때 입력 문장에서
내가 어디를 봐야 되는지를 학습하는 것이고
이렇게 이런 alignment 형태로 시각화가 된다고
이해하시면 되겠습니다.
기존에 sequence to sequence attention을
활용했던 예시들을 봤는데
이건 기본적으로 이전 시간에 배웠던
R&N 구조 위에다가 attention을 추가한 형태였죠.
그래서 attention이 그 중요한 역할을 해주긴 했지만
기본적인 모델 뼈대는 R&N 구조라는 것을
알 수가 있습니다.
근데 이제 2017년에 NIPS에
attention is only needed라는
인공지능계의 한 획을 크게 그었던
그런 논문이 발표가 되었는데
이 제목에서 알 수 있듯이
sequence, 우리가 모델이 sequence를 처리할 때
attention이면 충분하다라는 그런 메시지를
던지고 있습니다.
그래서 기존의 R&N 위에다가 attention을 붙였던 거에서
R&N 구조를 아예 제거를 하고 attention 자체를
중심 연산으로 사용을 하자라는 아이디어인데
그 모델 이름을 transformer라고
명명했습니다.
그래서 기존에 있었던
그런 sequence to sequence들이랑
새로 발표가 된 transformer가 뭐가 다른지를
살펴보겠는데
먼저 맨 처음에 attention이 없었던
sequence to sequence를 보면은
인코더 내부도 R&N이나 CNN으로 처리하고
그리고 decoder도 R&N이나 CNN으로
처리를 합니다.
그래서 이 인코더랑 decoder의
상호작용은 고정 길이의 백터 하나
인코더를 요약한 백터 하나를 압축해서
decoder에 넘겨주는 그런 구조였고요.
다음에 방금 슬라이드에서 예시로 봤던
attention을 사용한 sequence to sequence에서는
인코더랑 decoder 내부는 여전히
CNN이나 R&N을 사용을 하지만
그 인코더랑 decoder가 상호작용하는
방식이 attention을 도입한 거라고 볼 수 있습니다.
그래서 이렇게 attention을 도입하게 되면은
decoder가 attention을 통해서
인코더에 있는 모든 hidden state를
적절한 weight를 매겨서 참고를 할 수가 있었죠.
여기서는 attention이 추가가 되긴 했지만
여전히 뼈대는 R&N이나 CNN 같은
구조였다.
그런데 마지막에 있는 transformer 같은 경우는
이 세 개가 다 attention이라고 되어 있습니다.
그래서 인코더 내부랑 decoder 내부도
다 attention으로 처리가 되고
그 둘 사이의 interaction도 attention으로 이루어지게 됩니다.
그래서 이전에는 attention을 보조적인
그런 장치로 썼다면 이제 transformer에서는
메인 모델 전체를 구성하는 거를
attention으로 바꿔주게 된 거죠.
그래서 여기서 약간 들 수 있는
의문으로는 transformer가 진짜 그냥 attention
계산만 하는 거냐 라고 생각하실 수도 있는데
그건 아니고 기본적인 feedforward layer라든지
아니면 residual connection 같은 것들은
들어가 있다고 볼 수 있습니다.
하지만 핵심 연산이 R&N이 아니라 attention이기 때문에
only attention이라고 표현을 하고 있다
라고 생각을 해주시면 되겠습니다.
그래서 이제 이게 transformer
architecture 전체를 보여주는 그림이 되겠는데
왼쪽 부분이 인코더
이쪽이 인코더고 이쪽이
decoder 부분이라고 보시면 됩니다.
인코더랑 decoder 역할 자체는
아까 R&N이랑 동일한데
인코더가 입력 문장을 받아서 문맥이 반영된
표현을 만들어주고 decoder는 그 표현을
참고하면서 출력 문장을 한 단어씩
생성을 해주는 거라고 보면 됩니다.
먼저 맨 아래에 있는 입력 토큰
이 input들을 받아서
embedding을 거쳐줘요.
그래서 input embedding을 거쳐서 vector 형태로 바꿔주게 되고요.
그 위에 positional encoding이라는 게
들어가 있습니다. 우리가 이전에 R&N
할 때는 단어들을 순차적으로 처리를
해줬었잖아요.
근데 이제 transformer로 넘어가게 되면
그게 R&N처럼 순차적인 구조가 아니기 때문에
토큰의 위치 정보를 별도로
positional encoding을 통해서 넣어줘야 합니다.
그래서 positional encoding이 포함이 되어 있는 거고
왼쪽에 인코더
층이 이렇게 있죠.
그래서 이거를 보면은 옆에
곱하기 n이라고 적혀있어요.
그래서 인코더를 여러 번 층을 쌓는다라고 볼 수 있는데
인코더 하나의 구조는 이 네모난 박스 안에
들어있는 구조로 생겼습니다.
첫 번째로 인코더 들어갔을 때는
multi-add attention이 통과가 되고
두 번째는 feedforward network가
통과가 됩니다.
두 번째에는 multi-add attention을 거쳐서
feedforward network를 거친다라고
생각을 해주시면 될 것 같고
이 multi-add attention에서는 self-attention이라고 해가지고
입력된 토큰들이 자기들끼리 서로를 참고하면서
문맥 정보를 만들어내게 돼요.
그래서 이렇게 만든 문맥 정보를 가지고
feedforward로 올라가게 되면은
각 위치별로 한번 더 좀
그래서 보면은 토큰들이 서로를 보면서
self-attention을 만들어주게 되고
feedforward network로 올라가면은
좀 더 프로세싱을 해준다라는 느낌으로
이해를 하시면 되겠습니다.
그렇게 하고 오른쪽 decoder 부분으로
넘어가게 되는데 얘도 옆에 보면
곱하기 n이라고 적혀있죠.
그래서 decoder도 같은 층이 여러 번 반복되는 거라고
하시면 되겠고 내부는
이렇게 크게 3개라고
보시면 되는데 첫 번째가
얘도 마찬가지로
multi-add attention를 거치게 됩니다.
이때가 self-attention인데 decoder도
자기들끼리만 attention을 해서
하나의 그런
함출된 표현을 만들어주게 된다라고
생각을 하시면 되겠고
근데 여기서 여기
가로치고 masked라고 적혀있는 거는
출력 토큰들끼리
서로를 보는 게
self-attention인데 이제 미래에 있는 토큰들은
보지 않고 앞전에 있는 토큰들만 보겠다라는 게
masked의 의미가 됩니다.
그 다음에 위에 있는 decoder encoder attention
이 부분에서는
decoder가 encoder의 출력을 참고를 해서
표현을 생성을 하게 되고요.
이렇게 해서
현재까지 생성한 출력에 더해서
입력 문장 정보까지도 참고를 해서
encoder에 있는 부분도 참고를 해서
다음 단어를 예측을 해주게 되는 겁니다.
그리고 이렇게까지 됐으면 위로 올라가서
이런 구조인 feedforward 네트워크를 거쳐서
정보를 한번 더 처리를 해주게 됩니다.
그래서 보면은 이제
크게 그런 구조로 이루어져 있고 위에 이제
add and norm이라고 적혀있는 노란색 박스들이
붙어 있는데
이게 residual connection이랑 layer normalization 을 의미를 합니다.
이런 장치들을 해주는 거는
좀 더 학습을 안정적으로 만들어주기 위해서
붙여 놓은 거다라고 이해를 하시면 되겠고
이 decoder 위쪽에 보면은
또 linear랑 softmax 그리고
out probability로 올라가는 부분이 있는데
decoder의 최종 출력이 linear랑 softmax를 거쳐서
확률 상태로 또 바뀌게 됩니다.
합의 1인 distribution 상태로 바뀌게 되고
그걸 가지고 최종 단어
다음에 올 단어를 가장 확률이 높은 단어를
선택을 해서 다음 단어를 예측을 한다라고 보시면 되겠습니다.
그래서 transformer 구조를 크게 보면은
encoder에 있는 self attention
그리고 decoder에 있는 self attention
그리고 encoder decoder 사이에 cross attention
이 세 가지로 크게 구성이 되어 있다라고
이해를 하시면 되겠습니다.
그래서 우리가 이제 앞부분에서부터 계속
attention attention 해서 transformer까지 왔는데
여기서 이제 그 attention이 뭔지를
수식적으로 살펴보도록 하겠습니다.
처음에 이렇게 보면은 조금
복잡해 보이는데 실제로는
두 단계 정도로 이해를 할 수가 있습니다.
그래서 여기 나와 있듯이
search 단계랑 retrieval 단계라고
이해를 하시면 되겠고
search는 이제 내가 어디를 볼지 찾는 거고
retrieval은 그 다음에 거기서
정보를 가져오는 단계가 됩니다.
그래서 우리가 attention이라고 하면은
query key value로 표현을 해서
수식을 계산을 해주게 되는데
여기 나와 있듯이 search 단계는
query랑 key interaction이라고 보면 되고
retrieval 단계는 그걸 가지고 value 값을 가져오는
단계라고 이해를 해주시면 되요.
그래서 query라고 하는 거는 내가 지금
찾고 싶은 정보가 되고 key는
각 토큰이 가진 인덱스 정보라고
생각을 해주시면 되고 value는 그 토큰이
가진 실제 정보, 실제 값 내가 가져올
내용이라고 이해를 해주시면 되겠습니다.
그래서 여기 위쪽에 보면은
일단 맨 처음에 q랑 k를
계산을 하죠. 그래서 q랑 k를 계산을 해서
이 두 개가 얼마나 잘 맞는지
다 프로덕트를 하는 과정. 앞쪽에서 이제
여기 파란색 점을
만들어줬던 과정 있죠. 그게
attention 수식인데
q랑 k를 계산을 해서 얼마나 잘 맞는지를
봐줍니다. 현재 토큰이 다른 토큰들 중에
누구를 얼마나 참고해야 되는지를
점수로 계산하는 게 qk 적혀있는
부분이라고 볼 수 있고
그거를 보면은 root decay로 나눠져 있는 걸 볼 수 있습니다.
그래서 d는 차원인데
그 차원으로 나눠서 좀 스케일을 조절을
해주는 거라고 이해를 하시면 될 것 같아요.
그래서 점수 크기를 좀 스케일을 조절을 해주고
그거를 softmax를 태웠죠.
softmax는 합이 1인 Distribution으로
만들어주는 거라고 했는데
여기서도 softmax를 태워서 최종 attention score를
계산을 해주게 됩니다.
여기까지 있는 게 attention score라고
이해를 해주시면 되겠고
이렇게 attention score를 만들었으면 각 위치를
얼마나 어떤 비율로 참고할지에
대한 그 score를 만들어준 거니까
이거를 실제 value, 실제 값에 곱해서
최종적인 weighted가 반영된 값을
만들어줘야겠죠.
그래서 v를 곱해주게 됩니다.
이렇게 계산을 해주게 되면
중요한 위치의 value는 좀 더 높은 score로
많은 가중치를 가져서 더해지게 되고
덜 중요한 위치는 좀 더 낮은 score로
적은 weight가 곱해져서
더해지게 되겠죠.
이렇게 해서 attention score를
만드는 앞부분, v를 제외한 부분이
search 부분이라고 보면 되고
v를 곱해서 실제 value 값의 그 score를
곱해준 게 retrieval 부분이라고
이해를 해주시면 되겠습니다.
그리고 오른쪽에
multi-head attention 이런 게 나와있게 되는데
보통 이제 transformer에서도 그렇고
우리가 attention을 사용한 모델들에서도 그렇고
이런 attention 한 개만을 가지고
모든 모델이 잘 동작하게 되는 거는 아니거든요.
그래서 이런 multi-head attention이라고 해서
여러 개의 attention을 head 형태로
둬서 병렬적으로 수행을 하도록 만들어주게 됩니다.
실제 구현 단계에서는.
어떤 관계를 쓰냐고 한다면
이 head 하나하나마다 다른 관계를 볼 수 있을 거다
라고 기대를 하고 여러 개의 head를 쓰는 거고
다른 관계라고 하면 특정 head는
의미적인 관련성을 볼 수도 있고
특정 head는 문법적인 관련성을 볼 수도 있고
이런 식으로 다양한 관계성들을
attention이 잘 포착을 해주길 바라는
기대에서 이런 head를 여러 개 둔
multi-head attention을 사용을 하는 겁니다.
이렇게 여러 관점에서 attention을 수행을 해서
최종적으로는 그 결과를 concat하고
projection을 해서 최종 결과를 만들어주게 됩니다.
그래서 이제 self attention.
여기 구조에서 보면은
아래에 있는 multi-head attention들이
self attention에 해당했고
오른쪽 위에 있는 attention이
인코더와 디코더 사이에 cross model attention
cross attention이 된다고 얘기를 들었었는데
self attention에 대해서 조금 더 자세하게
보도록 하겠습니다.
그래서 핵심은
self attention의 핵심은 아까
attention식에서 볼 수 있었던 key query value 3가지 있잖아요.
그게 하나의 입력 x에서 나온다는 게
self attention의 특징입니다.
그리고 cross attention은 다른 두 개에서
key query value를 뽑아내서 정보를 섞는 거라고 볼 수 있고
self attention은 이제 하나의 x, 토큰 하나에서
key와 query와 value를 다 뽑아서
사용을 한다라고 이해를 하시면 되겠습니다.
그래서 왼쪽에 있는 이 x
이게 입력 토큰들의
representation을 모아둔 행렬이라고 보시면 돼요.
그래서 각 행이 하나의 토큰 벡터라고
이해를 하시면 됩니다.
그리고 중간에 있는 이 w
이 w가 학습이 가능한 가중치 행렬
이라고 이해를 해주시면 돼요.
그래서 이렇게
bilinear attention score 계산할 때
w라는 행렬을 중간에 뒀잖아요.
거기에 학습이 가능하다고 말씀을 드렸었는데
여기에서도 이렇게 학습이 가능한
이런 행렬 형태로 만들어주게 됩니다.
그리고 이 입력
입력 x에 각각 웨이트들이
q, query, value 웨이트들이 따로 있잖아요.
그거를 각각
곱해 주게 되면
q, query, key, value 값이
만들어지게 되는 겁니다. 그래서 x랑
가중치 행렬을 곱해 주는데
query에 해당하는 가중치 행렬을 곱해 준 게 q가 되고
k에 해당하는 가중치 행렬을 곱해 준 게 k가 되고
그리고 value에 해당하는 가중치 행렬을 곱해 준 게
v가 돼서 최종적인 key, query, value 값들을
만들게 되는 겁니다.
그래서 이렇게 만들어진 key, query, value를
위에 있는 attention 수식에 넣어주게 되면은
먼저 이 qk를
matchmul 한 걸로 토큰들 사이의
관련도를 계산을 해주게 되고
이거를 차원으로 나눠서 스케일을 조정하고
softmax로 확률 값으로 바꿔서
value 값을 곱해 주게 되면은 최종
attention이 생성이 되는 겁니다.
최종 출력이.
그래서 첫 번째 토큰 하나의
attention을 계산하는 과정을 조금 더 살펴보도록 하겠습니다.
그래서 이 q의
첫 번째 행을 떼어서
이게 토큰 하나에 대한 정보니까
이걸 가지고 attention을 계산을 해볼게요.
먼저
이 query를
모든 key, key는 지금
토큰 3개, 이게 지금 토큰 3개라는 의미가 되는데
key는 토큰 3개에 대한 정보를 다 가지고 있죠.
그리고 query도 다 가지고 있는데
그중에서 첫 번째 토큰에 대한 attention만
계산을 해본다는 의미입니다.
첫 번째 토큰 값과
토큰 전체 이 두 개를
계산을 해서 각각
이 첫 번째 토큰이
각 토큰하고 얼마나 관련이 있는지를
점수로 나타내 주게 되는 거죠.
결과 값이 244 이렇게 나와 있는데
이 말은 첫 번째 토큰이
첫 번째 토큰보다는 두 번째랑 세 번째 토큰에
조금 더 높은 관련도를 가진다는 의미가 됩니다.
그래서 여기 attention score가 되는 거고
이제 이 점수를 바로 쓰는 게 아니라
차원 크기로 나눠준다고 했었죠.
지금 보면 차원이 3차원이니까
이거를 root 3으로 다 나눠주게 됩니다.
여기다가 softmax를 적용을 해서
확률 값으로 변환을 해줘요.
그러면 지금 3개를 더하면 1이 되는데
합이 1인 확률 값으로 변환이 됐습니다.
이거를 해석을 해보자면
이게 attention weight가 되는데
첫 번째 토큰이 자기 자신은 13% 정도 참고를 하고
두 번째랑 세 번째 토큰은 각각
43% 정도씩 참고를 한다는 의미가 됩니다.
이렇게 가중치들이 나왔으면
이거를 실제 밸류들
아까 앞장에서 아래 구해놨던
이 밸류들이랑 곱해서
weighted sum을 해줍니다.
각 토큰이 가진 실제 정보가 밸류가 되는데
이거를 가중치를 곱해서
이 비율대로 섞게 되는 겁니다.
그러면 새로운 벡터가 나오죠.
여기 아래 새로운 벡터가 나오는데
이게 self attention를 거친
첫 번째 토큰의 새로운 representation이다
라고 이해를 해주시면 되겠습니다.
그리고 transformer가
transformer의 self attention 아까 해줬던 그 과정들이
각 토큰들이 다른 모든 토큰들을
동시에 참고하게 되는 거죠.
가중치 weighted score를 곱해서
동시에 참고하는 구조인데
이렇게 하면 토큰들 간의 관계는 볼 수가 있지만
순서 정보는 알 수가 없습니다.
예를 들어서
duck biteman이라는 문장이 있다고 했을 때
이게 순서가 바뀌어서 man by duck이라고
바뀐다고 하면 문장의 의미는 완전 달라지는데
같은 단어들로만 이루어져 있기 때문에
이 positional encoding을 하지 않고서는
의미가 반영이 될 수가 없게 되는 거예요.
그래서 각 토큰에 나는 지금 몇 번째
위치에 있다는 정보를 추가적으로 넣어주는 게
이 positional encoding 과정입니다.
그래서 원래의 논문
아까 attention is all you need라는 원래 논문에서는
각 위치에 대해서 사인이랑 코사인 암수로
이루어진 벡터로 만들어줬어요.
수식을 보면 위에 있는 짝수 차원에서는
사인 그리고 홀수 차원에서는 코사인에
사용을 하고
차원마다 홀수 짝수로 나눠서
차원마다 서로 다른 프리퀀시를
적용을 해주게 됩니다.
그래서 이렇게 하면 첫 번째 위치, 두 번째 위치,
세 번째 위치가 각각 다른 벡터를 가지게 되고
이 트랜스포머는 이 벡터를 통해서
토큰의 순서를 구분할 수 있게 됩니다.
그래서 실제로는 이 positional encoding을
해서 토큰 인베딩에 더해주게 돼서
그걸 입력으로 사용을 해주는 거예요.
그리고 또 이렇게 차원마다
다른 프리퀀시를 사용을 한다고 했는데
어떤 차원은 좀 더 짧은 거리에 민감하고
어떤 차원은 긴 거리에 민감하게
반응을 합니다. 그래서 위치도 위치인데
상대적인 거리 관계도 어느 정도 표현을 해줄 수가
있게 된다고 이해를 해주시면 되겠고
저게 아래 나와 있듯이 꼭
우리가 사인하고 코사인만을 이용해서
positional encoding을 해야 되는 건 아니고
이 원 논문에서도
학습이 가능한 positional embedding을 가지고
실험을 했는데 거의 비슷한 결과를 얻었다고
말을 해주고 있습니다.
핵심은 트랜스포머에 위치 정보를 꼭 넣어줘야 된다는 걸
기억을 하시면 되겠고
여기서는
self attention에서
퀄이랑 키랑 밸류가 각각 어떤 역할을 하는지를
그림으로 직관적으로
보여주고 있는데
여기서 중요한 거는 각 토큰이
attention 안에서 3가지 representation을 가져요.
각각 다른 역할을 한다는 게 핵심이라고
보시면 되겠고 첫 번째로 보이는
저 퀄이가 현재 토큰이 찾고 싶은
정보를 나타냅니다.
여기 아래 말하는 것처럼 예시가 적혀있는데
다른 토큰들한테 나한테 필요한 정보가
있는지를 물어보는 역할이라고 보시면 되겠고
키는 각 토큰이
어떤 정보랑 관련이 있는지를 나타내는
그런 표지판 같은
역할을 한다고 보시면 되겠습니다.
그래서 퀄이가 키를 보고
어떤 토큰이 중요한지를 판단을 하게 되는 거고
마지막으로 밸류는 그 토큰이
실제로 전달할 수 있는 information입니다.
오른쪽에 있는 그림을 보면
가운데 있는 퀄이가 모든 토큰의
키를 보고 관련도를 계산을 해주고 있죠.
그래서 이 퀄이가
노란색들을 보고
그거를 계산을 해주고 있습니다.
그리고 이 점수들을
소프트맥스로 바꾸면 각 토큰을
얼마나 참고할지가 결정이 되죠.
확률 형태로 노란색 값이 바뀐 것을 볼 수가 있고
이거를 가중치로 해서
밸류들, 빨간색 값 밸류들을
weighted sum을 해주면
내가 원래 이런 토큰에서 시작을 했는데
이 attention 과정을 쭉 거쳐서
새로운 표현인, 새로운 representation을
만들어주게 됩니다.
그래서 여기서도 볼 수 있듯이
지금 퀄이랑 키랑 밸류랑 모두 다
같은 문장에서 만들어졌죠.
지금 파란색, 빨간색, 노란색이
각각 퀄, 키, 밸륜데
다른 문장에서 가져온 정보 없이
한 문장 안에서 만들어진 값들입니다.
그렇기 때문에 우리가 이거를
self attention 이다라고 부르게 되는 거고요.
이 표는 이제 트랜스포머의
self attention을 RNN하고 CNN하고
비교를 해둔 표라고 볼 수 있습니다.
그래서 self attention이랑 RNN
이게 convolutional이 CNN이죠.
비교 기준은 총 3가지인데 첫 번째가
한 레이어당 계산량, complexity가 되고
두 번째가 연산의 수
순차적인 걸 처리할 때의
연산의 수, 세 번째가
최대 패스 랭크입니다.
첫 번째 있는 게 한 레이어를 계산할 때
얼마나 많은 연산이 필요한지를 나타내주게 되는 건데
self attention은 모든 토큰들이
서로를 보기 때문에 보면
O, N 제곱, D라고 나와 있어요.
그래서 이 시퀀스 길이가 길어질수록
비용이 제곱만큼
그냥 N인 것보다 훨씬
커지게 되는 거를 알 수가 있죠.
그런데 두 번째 열인
이 시퀀스 오퍼레이션을 보면
self attention은 O, N입니다.
이거는 토큰을 한 개씩 순차적으로 처리할 필요가 없이
전체 토큰 관계를 다 병렬적으로
처리를 해줄 수가 있다는 뜻이에요.
그래서 O, 1이 되고 반면에 RNN 같은 경우는
O, N이죠. 그래서 앞 시점이 끝나야지
다음 시점을 계산을 해줄 수가 있기 때문에
O, N의 그런 순차적인 연산을 필요로 하고
그렇기 때문에 병렬화로 만드는 데도
어려움이 있습니다.
세 번째 열인 맥시멈 패스 랭크는
다른 그 두 토큰이
서로 영향을 주기 위해서
몇 단계를 거쳐야 되는지를 나타내는 건데
self attention 보면 O, N이라서
만약에 좀 멀리 떨어져 있는 토큰이라고 하더라도
한 번에 직접 연결을 해줄 수가 있어요.
그런데 RNN 같은 경우는 우리가 히든 스테이트가
쭉 순차적으로 연결이 됐었죠.
그래서 O에 N이 걸린다고 볼 수 있습니다.
그래서 장거리 의존성을 학습하는 데에도
self attention이 훨씬 유리하고
물론 여기 맨 첫 번째에서 볼 수 있듯이
시퀀스에서 계산량은 커지는 단점은 있지만
병렬화가 쉽고
멀리 떨어진 단어를 직접 모델링할 수 있다는 점에서
큰 장점을 가지고 있는
아키텍처라고 볼 수가 있습니다.
그래서 우리가 이 self attention에 대해서
살펴봤는데 디코더에도 지금 두 개가 들어있죠.
그래서 아래쪽에 있는 거는
가장 동일한 self attention인데
지금 인코더에 있었던 거는
내가 지금 세 번째 토큰에 대한
가중치를 구하고 싶을 때에도
앞에 두 개 그리고 뒤에 세 개 토큰들의 정보를
다 사용을 할 수 있었는데
디코더에 있는 self attention은 만약에 여기에 있는
가중치를 구하고 싶다고 했을 때
뒤에 있는 토큰 정보는 보지 못하고
뒤에 있는 토큰들의 정보만 사용해서
self attention을 계산을 해주게 됩니다.
그래서 masked라고 붙어있는 이유가
previous 토큰만 볼 수 있기 때문에
이렇게 붙어져 있다고 이해하시면 되겠고
위에 있는 거는 cross attention
그러니까 디코더와 인코더 사이에
attention을 계산을 해주는 겁니다.
그래서 query는 디코더에서 가져오고
있는 값을 가져와서 인코더랑 디코더 사이에
얼마나 attention을 줘야 하는지를
나타내줄 수 있는 구조라고 보시면 되겠습니다.
이렇게 해서 트랜스포머 구조를 바탕으로
여러 모델들이 생겨나게 됐는데
대표적인 게 버트랑 GPT가 있습니다.
둘 다 트랜스포머 기반인데
트랜스포머 기반인데 학습 방식이
학습 방식이랑 그리고 내가 문맥을
보는 방식, 컨텍스트를 보는 방향이
다르다고 이해를 해주시면 되겠고
첫 번째에 있는 버트는 bidirectional
encoder representation from transformer의 약자입니다.
그래서 버트라고 이름이 붙어있고
여기서 핵심은
bidirectional 하다는 거
우리가 양방향 문맥을 볼 수 있다는 게 핵심이 됩니다.
그래서 버트를 학습시킬 때는
보통 이제 masked language 모델이라는
방식으로 학습을 하게 되는데
우리가 문장 중간에 빈칸을 만들고
그 자리에 어떤 단어가 들어가야 하는지를
맞춰주게 됩니다. 그래서 여기 아래 보면은
어제 카페 갔었어 그리고 빈칸
그리고 사람 많더라라고 입력을 줬을 때
이 빈칸에 해당하는 단어를
맞추도록 하는 게 masked language 모델 방식의
학습 방법입니다.
이제 이 빈칸을 추론할 때
앞에 어제 카페 갔었어 라는 문맥과
뒤에 사람 많더라라는 문맥을 둘 다
모두 볼 수 있기 때문에 이 특정 단어
거기 라는 단어가 들어가야 되죠.
이 특정 단어를 문장 전체 컨텍스트 속에서
이해하는데 유리한 그런 모델이라고
볼 수 있겠습니다.
반면 이거랑 대비되는 게 GPT
지금 CHPT에 근간이 됐던
GPT라는 모델인데 GPT는
generative pretrained transformer의 약자입니다.
이거는 앞 단어들이 주어졌을 때
다음 단어가 무엇인지를 예측하는
방식으로 학습을 하게 돼요.
Next Token Prediction이라는 방법론으로
학습을 합니다.
어제라는 단어가 주어졌을 때
카페라는 단어를 예측하고 어제 카페 단어가 주어졌을 때
갔었어 라는 단어를 예측하도록
학습이 된 거죠.
이거는 일방향, 양방향이 아니라 유니 디렉션하게
학습이 된 거라고 이해를 하시면 되겠습니다.
두 개를 비교, 정리를 하자면
버튼은 중간 빈칸의 기준으로
앞뒤 문맥을 다 활용해서 예측을 하게 한 거고
GPT는 앞에 있는 문맥만 보고
다음 단어를 예측하게 하는
어떻게 보면 생성 중심의 모델이라고 볼 수 있고
버튼은 문맥을 이해하는 것의 위주의
모델이라고 볼 수 있습니다.
뒤쪽에 버트랑 GPT를 조금 더
자세히 설명한 내용이 있는데
쉬는 시간 10분 정도 가지고
10시 반 부터 이어서 진행하도록 하겠습니다.
그러면 이어서 진행하도록 하겠습니다.
방금 전까지는
어텐션을 다뤄봤고
어텐션에 기반으로 한 트랜스포머 모델
그리고 트랜스포머 모델을 기반으로 한
버트랑 GPT 등을 간략하게 소개를 드렸고
버트에 대해서 조금 더 자세하게
살펴보도록 하겠습니다.
버트가 어떻게 트랜스포머 구조를 활용하고
어떻게 학습을 하고 있는지 살펴보도록 할게요.
일단 첫 번째로 가장 먼저 볼 점은
버트가 트랜스포머의 인코더 부분만
사용을 한다는 점입니다.
그래서 아까 이 구조 자체가 트랜스포머 구조에서
봤던 아키텍처랑 동일한데
디코더 부분이 다 X표시가 되어 있죠.
그래서 트랜스포머의 인코더만 사용을 하는 게
버트이다라고 이해를 하시면 될 것 같습니다.
그래서 사실 제가 앞전에 소개를 드릴 때
RNN에서도 그렇고
트랜스포머에서도 그렇고
인코더 부분이 내가 입력한 문장을 이해하고
함축된 표현을 만들어내는 부분이고
디코더는 그거를 바탕으로 어떠한 문장,
번역이라는 테스크면은 번역된 문장을
생성하는 부분이 디코더라고 했는데
그럼 디코더를 제외하고 인코더만 사용을 하는
모델은 어떻게 될까?
라고 생각을 해봤을 때
문장을 한 토큰씩 생성하는 모델이 아니라
그냥 입력 문장 전체를 보고
각 토큰의 문맥 표현, 컨텍스트 표현 등을
학습을 하는 모델이다라고
이해를 해주시면 좋을 것 같습니다.
그래서 인코더만 사용하기 때문에
버튼은 특정 단어를 해석할 때
왼쪽과 오른쪽 둘 다 볼 수 있겠죠.
디코더는 오른쪽이 있는 건 못 본다고 했었는데
인코더만 사용하기 때문에 왼쪽과 오른쪽
문맥을 둘 다 볼 수 있습니다.
그래서 양방향이라는 게 특징이고
가운데 있는 게 버트의 프리 트레인 과정을
보여주는 그림입니다.
앞전에 잠깐 말씀드렸듯이
대표적인 트레이닝 목표, 오젝티브가
마스크드 랭귀지 모델링이라는 방식으로
학습이 된다고 했었고
그때는 문장의 일부를 가리고 빈칸에 들어갈
단어를 맞추게 됩니다.
이 과정에서 모델이 양쪽 컨텍스트를 활용해서
언어의 표현을 학습을 하는
언어를 좀 더 잘 알게 되는 모델로
변해가는 과정이 프리 트레이닝 단계라고
볼 수 있겠고
그리고 이런
프리 트레이닝 단계에서는
어떠한 라벨이 붙은 텍스트가 필요가 없죠.
그냥 인터넷 상에 있는 수많은 글들에서
빈칸을 몇 개 뚫어서 그 빈칸을 맞추도록
학습이 된 것이기 때문에 엄청나게 많은 양의
비 라벨 된 텍스트로 프리 트레이닝이 가능했습니다.
그렇게 프리 트레이닝을 마치고
오른쪽 단계로 넘어가서 파인 튜닝을 하는
단계로 넘어가게 됩니다.
그래서 다양한 NLP 테스트들
NLP라고 하면
자연어 처리하는 그런 테스트들에 맞게
파인 튜닝을 해주게 돼요.
문장을 분류한다거나 아니면 객체명을 인식한다거나
아니면 질의응답을 한다거나
이런 특정한 테스트에 대해서 파인 튜닝이 됩니다.
그래서 이렇게 프리 트레이닝 단계로
대량의 텍스트로 넣은 문맥 정보를 이해해야 하는
버트라는 모델을 만들어두고
내가 풀고 싶은 특정 테스트에 맞춰서
위쪽에 조금 헤드 같은 것을 붙여요.
그래서 이거는 지금
빈칸을 채우는 식으로 학습이 되어있기 때문에
그때 사용했던 헤드는 다 걷어내고
안에 핵심 모델들, 그러니까 문맥을 잘 이해하고 있는
그런 핵심 웨이트들만 가지고
내가 원하는 테스트에 맞는 헤드를
다시 붙여서 파인 튜닝을 시키는 과정이라고
이해하시면 되겠습니다.
그래서 트랜스포머 인코더를 사용해서
양방향 언어 표현을 먼저 학습하고
그거를 여러 가지 다운 스트림 테스트에 대해서
재사용한다고 이해를 해주시면 되겠고
이제 이 버트가 학습될 때
마스크 랭귀지 모델링 방법으로
학습을 한다고 말씀을 드렸죠.
그래서 버트가 처음부터 감성,
그런 텍스트들의 감성을 분석한다든지
아니면 개체명을 인식한다든지
이런 특정한 테스트를 배우는 게 아니라
그냥 일단은 다량의 일반 텍스트를 읽으면서
언어 자체를 이해하는 능력을
학습을 해주는 게
Pre2n의 마스크 랭귀지 모델링 단계다
라고 생각을 하시면 되겠고
예를 들어서 이런 My dog is hairy라는
문장이 있다고 했을 때
여기에다가 마스크를 씌워두고
이거를 예측을 하게 만드는 거죠.
근데 이렇게 문장들을 만들어서 학습을 시킬 때
한 80% 정도만
마스크로 바꾸고
10%는 엉뚱한 랜덤한 월드로 바꾸고
10%는 그대로 둔 상태로
학습을 하게 됩니다.
이렇게 하는 이유는 모델이 너무 마스크 토큰에만
과하게 의존하게 될까 봐
이런 식으로 조금의 랜덤성도 주고 정답도 주는 식으로
데이터를 구성을 해서
학습을 하게 된 거고
맨 아래에 센텐스 A랑 B의
연관성 추측이라고 적혀 있는데
이거는 Next Sentence Prediction이라고 해서
두 문장이 실제로 이어지는 관계인지를
맞추는 그런 테스크도
Pre2n 단계에서 학습을 하게 됩니다.
그래서 이런 식으로
단어 수준에서의 빈칸 뚫린 단어
그리고 문장 수준에서의 관계까지
다량의 데이터를 학습이 된 게
버트의 Pre2n 된 모델이라고
볼 수가 있어요.
버트 헤드 쪽 보면 NSP 헤드가
센텐스 A랑 B의 연관성을 추측하는
그러니까 Next Sentence Prediction의 헤드라고
보면 되고 중간에 있는 게
마스크 랭귀지 모델링 빈칸을
뚫어서 그거를 맞추는 헤드를 붙여서
이런 방식으로 학습이 되었다.
그래서 이렇게 Pre2n이 끝난 버트를
실제 테스크, 내가 풀고 싶은 테스크에 맞게
Fine2n 하는 과정을 보여주는 표입니다.
이전 단계에서
내가 두 문장 간에 연관이 있는지랑
그리고 어떤 문맥 사이에서 빈칸을 채우는
그런 능력들을 학습을 하면서
언어 표현 방법들은 잘 배워놨기 때문에
이제는 그 위에 간단한
테스크 스페스피크한 아웃풀 레이어만 붙여서
다양한 NLP 문제들을 풀게 만들 수 있습니다.
그래서 왼쪽에 있는
이 예시 같은 경우는 문장 전체에 대해서
하나의 레이블, 지금 부정이라고
되어 있는 하나의 레이블을 예측하는 경우입니다.
그래서 대표적인 테스크로는
음성 분석 같은 게 있을 수가 있어요.
문장이 긍정적인지 부정적인지를 분류하는
그런 테스크로 학습을 시킬 수가 있습니다.
이런 것을 할 때는
문장 앞에다가 CLS라는 토큰
representation을 붙여서 최종적으로
CLS라는 토큰이 부정 혹은 긍정으로
예측이 되도록 학습을 시킬 수가 있겠습니다.
여기서는 부정이라는 결과를
예측을 하고 있고요.
오른쪽 예시를 보면
각 토큰마다 태그를 붙이는
sequence labeling 문제입니다.
이거의 대표적인 테스크 이름은
개체명 인식하는 테스크가 있는데
각 단어가 사람 이름인지 기관 이름인지
이런 것들을 판단하는 문제입니다.
그래서 삼성이라는 단어가 기관의
이름이 있다라고 예측이 되도록 학습을 시킨 거죠.
여기서는 CLS 토큰만
이것만 쓰는 게 아니라
각 토큰의 최종적인 hidden representation들이
attention score 쫙 계산해서 나올 텐데
이걸로 각 위치마다
별도의 라벨을 예측하도록 한 겁니다.
그래서 벌트 본체는
그대로 둔 상태로
원하는 CLS 토큰을 활용하고
토큰을 분류하는 거면
각 토큰의 출력을 이용해서 어떤 결과를
만들어내는 식으로 테스크에 맞게
output head만 다르게 붙여서 학습을 한 게
fine tuning with BERT라는 것을
알고 넘어가면 될 것 같습니다.
그래서 이렇게 BERT를 알아봤고
그 다음으로는 GPT
Generative Pre-trained Transformer라는 GPT를
살펴보겠습니다.
그래서 이전에 BERT는
트랜스포머의 디코더 부분을 다 제외하고
인코더 부분만 사용을 한다라고 말씀드렸는데
이 GPT는 트랜스포머의
디코더 부분만 사용을 한다고
트랜스포머의 디코더 부분만 사용을 하는
구조입니다. 그림에서도
인코더는 다 제외가 되어 있고
그렇기 때문에 인코더랑 크로스어텐션을 했던
이 부분도 제외가 되어 있고
디코더 부분만 남아있는 것을 볼 수 있습니다.
그래서 디코더 언니
디코더 언니 구조이기 때문에 GPT는
Masked Self-Attention을
사용을 하게 돼요.
이전에 제가 이쪽에서는
내 앞에 있는 토큰들만 볼 수 있는
Self-Attention의 종류 중에 하나인
Masked Self-Attention을 사용한다 라고 말씀드렸었는데
GPT는 그 Attention만을 사용하고 있습니다.
그래서 현재 위치에서는
미래 토큰을 볼 수 없게 그래서
앞 단어들만 보고 다음 단어를 예측하도록
그렇게 학습을 하게 됩니다.
그래서 오른쪽 그림도 보면
인프 센텐스가 있고 이렇게 예측해야 되는
아웃풋 토큰, 아웃풋 단어가 있을 때
GPT를 프리 트레이닝하는 과정에서
앞에 있는 토큰들만 보고
다음 단어가 무엇인지를 예측을 하도록
Next Token Prediction을 대규모 텍스트에
반복해서 학습을 했습니다.
그래서 앞에 버튼은
좀 더 다양한 컨텍스트를
이해하는, 이해 중심의 모델이고
그걸 활용해서 어떤 Fine Tuning을 시켜서
내가 원하는 테스크를 수행할 수 있도록
하고 한다면 GPT는 Decoder Only 기반의
생성 중심 모델이라고 볼 수 있겠습니다.
그래서 다음 토큰을 계속 예측하는
그러니까 GPT가 하는 것처럼
내가 어떤 어떤 문장을 입력을 했을 때
그에 맞는 문장들을 쭉 생성하기에 적합한
생성 중심의 모델이라고 볼 수 있습니다.
그래서 이렇게 트랜스포머를 활용한
모델들을 봤는데 앞에 두 개 모델은 둘 다
자연어 처리를 할 수 있는
그런 모델들이죠. 그래서 트랜스포머가 처음에는
이렇게 자연어 처리를 하는 데 특화된
방법으로 소개되고 활용이 됐었는데
이제 이후에는 이게 NLP에서 벗어나서
비전이나 멀티모델 분야까지
다 트랜스포머 기반의 모델들로 확장이
되었습니다. 그래서 비전에 적용된다고 하면은
이미지를 패치 단위로 나눌 수가 있잖아요.
그 패치를 토큰처럼
벡터화를 해서 트랜스포머에 넣습니다.
그래서 앞전 NLP에서 단어들을
시퀀스로 처리를 하듯이 이 이미지도
패치 단위로 만들어서 시퀀스로 바꿔서
처리를 해주게 되는 겁니다.
그래서 원래는 CNN 모델 이전 시간에 배우셨을
CNN 모델들을 이용해서 비전 정보를
많이 처리를 했었는데 이제는 비전
트랜스포머라고 해서 VIT 같은 모델들이
등장을 했고 그것들을 활용해서
이미지 클래시피케이션도 하고 오브젝트
디텍션이나 이미지 캡셔닝이나 아니면
인스턴스 세그멘테이션 같은 비전 고유의
문제들도 트랜스포머 기반의 모델들로
활용을 하고 있습니다.
그리고 멀티모델 같은 경우에도
텍스트랑 비전이 같이 들어가는
멀티모델 모델들 같은 경우에도
텍스트는 이미 토큰 형태니까 사용을 하고
그리고 이미지는 패치 토큰으로 만들어서
함께 입력하거나 둘 사이에
어텐션들을 계산해서 관계를 학습을 할 수 있도록
구조를 만들어뒀습니다.
그래서 이런 트랜스포머 기반의
멀티모델들로
이미지 텍스트 매칭하는 것
아니면 비쥬얼 그라운딩 하는 것
텍스트에 있는 이미지를 보고
이렇게 이런 텍스트를 보고 이미지에서
이 텍스트가 의미하는 영역을 찾아내는
그런 비쥬얼 그라운딩이나 아니면
VQA 비쥬얼 퀘스트 앤서닝 같은
문제들 그리고 이미지 캡셔닝 같은
작업들을 가능하도록 만든 게
트랜스포머 구조입니다.
그래서 처음에는 이제 단어들 사이의 관계를
잘 학습해보자라는 목적으로 트랜스포머가 나왔지만
이제는 서로 다른 모달리티 사이의 관계까지
학습하게 되는 그런 범용적인
아키텍처로 발전을 했다고
이해하시면 되겠습니다.
여기까지 이제 트랜스포머에 대한
개우를 맞춰봤고 이제는 실습 파일로
넘어가서 같이 실습을 통해서
아키텍처들을 살펴보도록 하겠습니다.
그래서 랩 3-1 트랜스포머 파일을
열어주시면 되겠고
오늘 중점적으로 해볼 거는
이제 어텐션.
저희 아까 수식으로 다뤄봤던 어텐션을
코드 레벨로 표현할 수 있을지를 자세히 살펴보도록 하겠고
그리고 시퀀스 투 시퀀스에
어텐션을 적용했던 모델이 있었죠.
슬라이드 초반에 있었던
그거를 직접 구현하고 학습시켜보는 것까지
진행을 해보겠고
그 밑에 이제 트랜스포머 아키텍쳐도 전부
전체 아키텍쳐도 제가 붙여놨는데
이거는 직접 코드를 짜보는 단계보다는
설명 위주로 코드를 짚고
넘어가는 식으로 마무리하겠습니다.
그래서 맨 처음 이제 어텐션부터 살펴볼 건데
그 전에 우리가 이전 시간에도 했었던
필요한 모듈들, 임포트해주는 작업이 필요하겠죠.
그래서 모델 구조들을 위해서
토치랑 토치점에는 임포트를 해주고
그리고 이거는 이제 학습할 때
시간축 같은 걸 나타낼 수 있는 모듈이라서
임포트를 했습니다.
그리고 토치도 임포트를 해주고요.
혹시 글자가 너무 작은가요?
괜찮나요?
그러면 첫 번째로 이제
모델을 만들어 보는 건 아니고
어텐션을 어떻게 구현할 수 있는지부터
살펴보도록 하겠습니다.
그래서 우리가 아까 슬라이드에서 보면은
이거 해봤었죠.
그래서 이거를 코드로 구현을 해보도록 할게요.
일단 처음에 X
우리가 생각했을 때는 기계변역을
예시로 든다면 단어 3개로 이루어져 있는
그러니까 토큰 3개를 가지고 있는
벡터를 만들어줍니다.
그래서 이 3개가
단어 3개를 가지고 있는
벡터를 만들어줍니다.
그래서 길이는 총 3개고
각 토큰이 4차원 벡터를
가진 형태로 X를 만들어줬고요.
그 다음에 어텐션 계산을 위해서
필요한 가중치 행렬을 만들어줬습니다.
그래서 퀄이의 고패질 가중치 행렬
W 퀄이를 만들어줬고
이거는
4X3으로 만들어줬어요.
그리고 키에 해당하는
가중치 행렬도 만들어줬고
밸류에 해당하는 가중치 행렬도
만들어줬습니다.
그래서 아까 슬라이드에서는 제가 이 벡터들이
학습 가능한 런어블한 벡터들로 구성을 할 수 있다
라고 말씀드렸었는데
여기에서는 계산을 위해서 이미의 값들을 넣어뒀다
라고 생각하시면 되겠습니다.
그래서 행렬들과 X값 만들어서
실행을 시켜주고
이거를 사용해서 키 퀄이 밸류 값들을
계산을 해볼 수가 있죠.
그래서 이거를 제가 투드라고 적어놨으니까
이 행렬급을 통해서 각 토큰의
키와 퀄이와 밸류를 생성하는 부분을
코드 작성을 해보시기 바랍니다.
그래서 한 3분에서 5분 정도의
시간을 드릴 테니까 한번 코드 작성을
해보세요.
같이 한번 작성을 해볼게요.
그래서 우리가 슬라이드
여기 슬라이드에서 연습을 할 때는 각각의 토큰 하나씩 가져와서
그거를 행렬을 곱해서 계산을 해주고 했었는데
이 세 가지의 토큰들을 한 번에 계산을 할 수도 있겠죠.
행렬 곱을 사용하면 행렬 길이의 곱을 나타낼 수가 있으니까
그거는 토치점 매트물이라는 함수를 사용해서 작성을 해줄 수가 있습니다.
행렬 곱에 대상이 되는 두 가지, 첫 번째 키를 만들어줄 때는
우리가 가지고 있는 X값이랑 만들어줬던
만들어줬던 키에 해당하는 이 가중치 행렬을 곱해주면 되겠습니다.
그래서 X랑 W키를 매트물 안에 넣어주도록 할게요.
그리고 퀄이도 비슷하게 적어줄 수가 있을 텐데
X 그리고 W퀄이를 사용해서 X랑 가중치 행렬을 곱해서 퀄이를 만들어주겠습니다.
그리고 벨류도 동일하게 적어주면 되는데
X랑 W, 벨류를 사용해서 만들어줄 수가 있겠습니다.
한 번 프린트 키만 해볼게요.
그렇게 됐을 때 지금 X 3X4랑 W키 4X3를 곱해줬죠.
그래서 최종적으로 3X3의 키가 생성이 됐고, 퀄이랑 벨류도 마찬가지일 겁니다.
그래서 이런 식으로 행렬 곱을 통해서 각 토큰의 키 퀄이 벨류를 생성을 했고
여기서 중요한 것은 우리가 지금 셀프어텐션을 구현을 하고 있는 거라고 볼 수 있겠죠.
왜냐하면 키랑 퀄리티 벨류를 다 X를 통해서 만들어줬기 때문에
이거는 한 문장 안에서 어텐션 값을 계산하는 셀프어텐션의 계산 과정이다라고 이해하시면 되겠습니다.
이렇게 계산을 해줬으면 우리가 어텐션 스코어를 계산해 줄 수가 있겠죠.
그래서 어텐션 스코어는 어떻게 계산하면 될지를 생각을 해보시고
아까 여기서 보면은 이 과정이 아니라
이 과정, 이거를 구현을 해서 어텐션 스코어 자체를 만들면 되겠습니다.
이것도 투드로 적어놨으니까 2, 3분 정도 시간 드릴 테니까 코드 작성을 해보시기 바랍니다.
bé즈 mourn
사실 이거는 임의로 과정 질을 설정해둔 거라서 큰 의미는 없긴 하지만
밸류 값은 실제 값이다 보니 조금 더 큰 값을 높여서 차이를 보여주려고 큰 값을 올린 것 같아요.
값을 높여서 차이를 보여주려고 큰 값을 올린 것 같아요.
값을 높여서 차이를 보여주려고 큰 값을 올린 것 같아요.
값을 높여서 차이를 보여주려고 큰 값을 올린 것 같아요.
네, 지금까지 저희가 PPT에서 보면 오른쪽 위에 있는 키, 퀄이 밸류를 각각 만들어줬고,
이제 이 단계, 퀄이랑 키의 트랜스폼을 내적해주는 단계를 작성을 해주면 되는데,
이것도 행렬 곱 형태로 만들어줄 수가 있습니다.
그래서 토치점 매트물을 이용해서 곱해줘야 되는 게 퀄이랑 키였죠.
그래서 시계에서도 볼 수 있듯이 퀄이랑 키의 트랜스폼을 한 형태의 행렬을 곱해주면 되는데,
이거는 위에서 만들어놨던 이 퀄이지랑 키의 티를 붙여서 트랜스폼을 만들어줄 수가 있습니다.
그래서 이 두 개를 매트물 한 개의 어텐션 스코어가 되고,
이거를 출력을 해보면 지금 3x3의 형태의 텐서가 등장하게 됩니다.
그래서 이게 토큰 갯수랑 토큰 갯수라고 보면 되는데,
퀄이도 토큰들에 해당해서 3개의 값이 있었고, 키도 3개의 값이 있었죠.
그래서 그거를 매트물을 해서 이렇게 나타나지게 됩니다.
행이 기준이 되는 퀄이 토큰이라고 보면 되고, 열은 참고 대상이 되는 퀄이 토큰.
그래서 i,j는 i번째 토큰이 j번째 토큰을 얼마나 볼지를 어텐션 스코어를 계산해준 게 이 결과라고 볼 수 있습니다.
이거는 첫 번째 단어가 자기 자신을 얼마나 볼지, 첫 번째 단어가 두 번째 단어를 얼마나 볼지를 나타내준 어텐션 스코어를 계산해봤고요.
이제 이거를 가지고 루트 차원으로 나눠주어서 정규화를 시켜주고,
이거를 소프트맥스에 태워주는 작업을 해야겠죠.
그래서 일종의 확률분포처럼 합이 1위 되는 디스티비션으로 만들어줘야 합니다.
그 과정을 또 제가 여기 투두를 걸어놨으니까 한번 코드 작성해보시기 바랍니다.
그래서 이거는 이제 우리가 계산해둔 이 윗부분을 계산해 줬으니까 디멘션에 루트를 씌워서 나눠주는 것부터 시작을 해야 되는데,
디멘션 값을 가져와 줄 때는 우리가 위에다가 키 만들어 뒀잖아요.
그래서 이렇게 지금 텐서가 출력이 됐는데, 3x3의 텐서라고 볼 수 있고,
여기서 디멘션은 뒤에 있는 이 3이라는 값을 가져와서 활용을 해주면 됩니다.
그래서 키즈의 쉐이프에서 맨 뒤에 있는 값을 가져올 때는 마이너스 1 음의 인덱싱으로 가져와 줄 수가 있죠.
그래서 이렇게 디멘션을 가져와 주고 여기에다가 루트를 씌운 값을 만들어야 되니까 numpy에서 함수를 사용을 해서 감싸주고,
루트 씌운 디멘션 값을 만들어주겠습니다.
그리고 아래에 softmax 씌우는 부분은 작성이 되어 있는데,
softmax 함수가 내장이 되어 있어요.
그래서 torch.nn.functional에서 softmax라는 함수를 가져와서 사용을 해주면 되겠고,
이제 우리가 위에서 구해둔 확률분포 합이 1이 되기 전 attention score를 가져와 주고,
그거를 아까 우리 만들어둔 루트 씌운 디멘션으로 나눠주겠다.
그리고 그 값에다가 softmax를 취해 줄 건데,
여기 디멘션이라고 적혀있는 게 마이너스 1로 되어 있죠.
이것도 맨 끝에 차원을 가져와서 softmax를 취해주겠다는 뜻입니다.
첫 번째 시간에 numpy 실습에서도 축을 기준으로 여러 가지 연산을 해줬었는데,
이것도 축을 지정해주는 거라고 볼 수 있겠고,
축을 지정해주지 않으면 모든 단어들이 섞여서 계산이 되겠죠.
우리는 지금 토큰 하나하나마다 softmax를 계산을 해주고 싶기 때문에,
축을 지정해준 거라고 볼 수 있겠습니다.
이렇게 해서 실행을 해보면 지금 합이 1이 되는 값 형태로 나와 있고,
얼추 보면 4 더하기 4 더하기 1이니까 9. 몇 해서 총 합이 1이 되겠고,
그런 식으로 작성을 해볼 수가 있겠습니다.
그리고 그 다음으로 지금 여기까지 한 거면 이 attention에서 softmax 한 것까지를 계산을 해준 거거든요.
실제 attention 스코어를 가지고 우리가 가지고 있는 value 값에다가 곱해주는 작업을 해야지
최종 attention이 반영된 representation을 만들어줄 수 있기 때문에,
가중치를 곱하는 부분 또 to do를 걸어나서 여기를 작성을 해보시기 바랍니다.
감사합니다.
그래서 value를 계산할 때도 여기서도 행렬의 곱으로 계산을 하는 거기 때문에,
토치점, 매트물 함수를 사용을 할 거고,
위에서 softmax를 취해준 attention 스코어랑 그리고 위쪽에서 우리 만들어놨었던 value에 대한 가중치 행렬 곱해서 만들어둔 values라는 값 있죠.
그거를 두 번째 인자로 넣어서 둘 사이에 행렬 곱을 구해주도록 하겠습니다.
실행을 해보면 아까 구했던 attention 스코어랑 내가 실제로 가지고 있는 value의 값을 가중합한 결과가 잘 출력이 돼요.
그래서 실제 모델에서는 인풋이 이런 식으로 x로 들어오게 된다면,
내부적인 attention 계산을 통해서 우리가 어느 부분에서 참고할지가 반영된 attention weight가 반영된 value 값으로 출력이 되고,
이거를 활용을 해서 decoder 단으로 넘어가서 다음 단어가 무엇이 와야 할지를 예측을 하는 거라고 볼 수 있겠습니다.
이렇게 기본적인 attention 구조를 조금 코드 레벨로 이해해보는 시간을 가져봤고,
이제 해볼 거는 지난 시간에 했던 sequence to sequence 모델 있죠, RNN 모델 여기에다가 attention을 입히는 작업을 해보도록 하겠습니다.
이거를 통해서 해결해볼 테스크는 프렌치로 작성된 문장을 영어로 바꾸는 기계번역 테스크를 sequence to sequence with attention 모델로 해결을 해볼 거고,
이런 식의 문장이 들어온다면 정답이 이렇게 되는데 출력 문장도 정답과 가까운 문장을 뱉도록 학습이 되어야겠죠.
두 번째도 이런 프렌치 문장이 들어갔을 때 이런 영화 문장이 정답이다 라고 한다면 모델의 출력도 같은 정답으로 출력되도록 학습을 시켜보도록 할게요.
일단 첫 번째로 데이터 준비해주는 부분 시작을 해볼 건데 그 전에 여기서도 모델을 import를 많이 해주게 됩니다.
기본적인 토치모델들 그리고 numpy 그리고 데이터셋이나 데이터로더 관련된 모듈들 쭉 import를 해주고요.
그 다음 디바이스를 모델을 학습시킬 때에는 gpu로 지정을 해줄 것이기 때문에 torch.device를 지정해주게 되고,
이 말은 사용할 수 있는 gpu가 있으면 사용을 하고 아니면 cpu에 올려라 라는 말입니다.
그래서 여기까지 셀 하나를 실행을 해주고 두 번째로 우리가 해줘야 될 게 데이터 단계에서 해줘야 될 게 사전을 하나 만들어줘야 합니다.
그래서 입력되는 그런 문장들이 있을 텐데 그거를 기반으로 사전을 만들어주는 함수가 구현이 되어 있다고 보면 되고,
여기에 내용으로는 단어를 어떤 인덱스로 바꿔주는 word to index를 해서 값들을 모아줄 거고,
그리고 해당 단어의 등장 횟수들도 모아줄 거고, 그 다음으로는 인덱스를 다시 단어로 만들어주는 딕셔너리도 만들어줄 거고,
그 다음에 전체 보케블러리의 크기도 활용할 일이 있어서 그 크기도 세워주도록 하겠습니다.
크기는 처음에 2로 초기화 되어 있는 거는 sos 토큰과 eos 토큰이 기본적으로 포함이 되어 있다고 보면 되고,
이거는 start of sentence, end of sentence 문장의 시작과 끝을 나타내 주는 토큰들은 처음에 디폴트 값으로 넣어주고 시작을 합니다.
그래서 이 클래스에 있는 기능으로는 sentence가 들어오면 개를 공백 단위로 쪼개서 단어 단위로 만들어주는 함수가 하나 있고,
그리고 위에서 우리가 필요로 했던 이 딕셔너리들 있죠? 그것들을 채워주는 함수가 구현이 되어 있습니다.
그리고 이 아랫부분은 이제 입력되는 그런 프랑스 문장들을 깔끔하게 정리해주는 작업들이라고 생각을 해주시면 되는데,
위에 있는 함수는 이런 식의 문자가 들어왔을 때 그냥 깔끔한 이 형태로 바꿔준다든지 그런 부분이고,
다음으로는 문장들에 있는 알파벳들을 다 소문자로 바꿔준다든지 아니면은 레터가 아닌 것들, 특수문자들을 삭제해준다든지 하는 작업이라고 볼 수 있습니다.
그래서 우리가 모델을 학습시키기 위해서 좀 더 깔끔하고 정제된 문장들을 만들어준다라고 이해를 하시면 될 것 같고,
이것도 실행을 시켜줄게요.
그리고 여기서 중요한 게,
이제 학습을 시킬 문장들을 읽어와서 걔네들을 한 쌍의 페어로 학습하기 좋은 문장 페어로 만들어주도록 할 텐데,
그러기 위해서는 이 English to French라는 텍스트 파일을 코랩에 업로드를 시켜주셔야 합니다.
여러분들도 구글 코랩 파일에 아마 이 ENG French 텍스트 파일이 있을 거예요.
그래서 그거를 컴퓨터에 다운로드를 시켜주시고,
그 다운로드 받은 파일을 코랩에 보면 왼쪽에 파일 버튼 있죠?
이거 눌러서 업로드 버튼 눌러서 이 파일을 업로드 시켜주도록 하겠습니다.
이 파일은 이제 런타임이 돌아가는 동안만 내부 저장 장치에 저장이 되어 있을 거고,
이거를 활용해서 저희는 학습을 시켜보도록 할 겁니다.
혹시 이 파일 못 찾으신 분 있으신가요?
구글 코랩 아까 이 트랜스포머 실습 파일 있는 폴더에 제가 올려놨거든요.
지금 다운로드 받은 파일을 어떻게 업로드 하세요?
여기 컨텐츠 안에 들어가요.
여기에다가 올려주시면 돼요.
그래서 다들 찾고 업로드 해주셨죠?
파일 살펴보면 영어 문장이랑 거기에 대응되는 프랑스 프렌치 문장으로 구성이 되어 있는 파일이라고 보면 됩니다.
그런 텍스트 파일이고 우리는 영어 문장 혹은 프랑스 문장이 입력이 됐을 때
그거를 서로 다른 언어로 번역을 해주는 모델을 구현을 해보려고 하는 거고,
그 파일을 읽어주는 함수가 구현이 되어 있습니다.
그래서 위에 지금 노멀라이즈 스트링이나 아니면 유니코드 투어 아스키 같은 함수줄 구현해뒀는데,
이 파일에서 문장 읽어와서 이런 함수들, 아까 구현해뒀던 함수들을 적용을 해서 깔끔한 문장으로 만들어주고,
그리고 리턴이 지금 인풋 문장과 아웃풋 문장 그리고 그거의 페어로 된 거를 리턴을 하도록 만들어져 있는 함수예요.
그래서 번역 모델이 결국은 입력 문장이랑 출력 문장의 쌍을 학습하는 Supervised Learning 문제잖아요.
그래서 그 형태로 아까 텍스트 파일을 가공해주는 작업이라고 볼 수 있겠습니다.
그래서 이 함수 보면 리벌스라는 파라미터가 있는데, 디폴트는 false로 되어 있고,
이게 리벌스가 추력값으로 들어오게 되면 문장의 순서가 바뀌는,
그러니까 처음에는 영어 그리고 프렌치가 쌍으로 학습이 돼서 영어 문장을 넣으면 프랑스어로 번역되게 학습이 되는 거라면,
리벌스 버전은 프랑스어가 입력되면 영어가 출력되도록 학습을 할 수 있는 데이터 쌍을 만드는 거라고 볼 수 있겠습니다.
그래서 요셀도 실행을 시켜주도록 할게요.
그리고 그 다음 셀은 원래 디폴트 학습에는 활용되지 않겠지만,
저희는 이제 조금 실습시간 내에 학습을 시켜줘야 되기 때문에,
비교적 단순한 문장들만 뽑아서 학습을 해주기 위해서 요런 셀이 작성이 되어 있고,
필터를 거쳐가지고 좀 짧은 문장들, 그리고 요런 프레픽스들이 포함되어 있는 문장들 위주로 선별을 해서 데이터를 구성을 하는 부분입니다.
그래서 학습에 필요한 데이터를 필터링 해주는 부분이라고 볼 수 있겠고,
이제 프리페어 데이터를 완성을 해줄 수가 있어요.
그래서 위에서 작성했던 함수들을 쭉 호출을 하면서 데이터를 준비해주는 과정이라고 볼 수 있겠고요.
요거를 실행을 해보시면 여러분들이 업로드했던 파일을 읽어와서 전처리들이 쭉 진행이 되고,
그거를 한 쌍의 페어로 만들어서 저장을 해주게 됩니다.
그래서 요런 커멘트들이 나오는 게 정상이고요.
다 잘 됐으면 마지막에 랜덤쯤 초이스로 랜덤한 페어를 뽑도록 되어 있기 때문에 예시 문장이 뽑혔을 겁니다.
그래서 프렌치 문장 있고 영어 문장 있는 예시 문장까지 뽑아줬으면 이 단계까지 잘 완료가 됐다고 볼 수 있어요.
혹시 여기까지 잘 안되시는 분 계신가요?
없죠? 그러면 넘어가보도록 할게요.
여기까지 데이터를 준비하는 과정이었고,
이제 이거를 사용을 해서 우리가 시퀀스 투 시퀀스 모델을 학습을 시켜볼 겁니다.
그래서 요 시퀀스 투 시퀀스 모델이랑 어텐션을 넣은 모델이랑 두 개 다 학습을 시켜서 비교를 해보는 거를 해볼거고,
첫번째로 어텐션이 적용되지 않은 기본 시퀀스 투 시퀀스 모델을 구현을 하기 위해서 인코더 단계랑 디코더 단계를 만들어줘야겠죠.
그래서 처음에 인코더 RNN이 나와있습니다.
그래서 여기 위쪽 이니트 부분에 파라미터들을 정의해주는 거 MLP 했던 거랑 동일하고요.
우리가 사용해줄 거는 인베딩 하는 부분 그리고 GRU라는 거는 RNN 계열의 모델 종류라고 보시면 되겠습니다.
그래서 여기가 모델 부분이라서 히든 사이즈 넣어주고 그런 부분이라고 볼 수 있고 드랍아웃도 적용을 시켜줄 수가 있어서 드랍아웃을 넣어놨는데
이거는 이제 내가 학습을 시킬 때 몇몇 노드들은 끈 상태로 학습을 시킬 수가 있어요.
그래서 랜덤하게 몇몇 노드들을 끄고 키고 하면서 좀 더 학습에 변동성을 주는 느낌으로 생각을 해주시면 될 것 같습니다.
드랍아웃이라는 스킬인데 이거는 학습을 더 잘하기 위한 일종의 스킬이라고 봐주시면 될 것 같고
그래서 밑에 이제 forward 함수라고 제가 적어놓고 to do를 걸어놨어요.
그래서 드랍아웃을 통해서 인베딩 하는 것까지 완료가 됐고 단어를 댄스 벡터로 변환하는 것까지는 해놓고
이제 forward 함수를 완성시키는 부분을 작성해보시면 되겠습니다.
이거는 attention 없는 기본적인 RNN 모듈을 모델을 구현을 해주시면 될 것 같아요.
그래서 이 부분 코드는 간단해서 같이 적어보도록 할게요.
일단 입력 값을 인베딩 형태로 만들어줬으니까 이거를 순서대로 시퀀스를 읽으면서 hidden state를 업데이트하는 부분을 적어주면 되는데
그거는 우리가 위에서 gru라는 모델을 가져왔죠.
그래서 이거를 활용해서 작성을 해볼 수가 있습니다.
그래서 모델의 아웃풋 자체가 지금 단계에서의 아웃풋 결과 그리고 히든 값을 뱉도록 되어 있는 모델이기 때문에
아웃풋과 히든을 출력을 해줄 거고
이제 셀프 점 gru를 가져와서 여기에다가 위에서 계산을 해준 인베딩 값을 넣어주면 됩니다.
그러면 모델의 아웃풋이 이렇게 저장이 되고 이거를 400 암수를 호출했을 때 리턴되는 값들로 적어주도록 할게요.
이렇게 해서 RNN 모델을 만들어주었고 다음에는 인코더를 만들었으니까 이제 디코더를 만들어줘야겠죠.
그래서 디코더 만들 때에도 비슷하게 작성을 해줄 수가 있는데 처음에 이닛 부분에서 필요한 파라미터들 적어줬죠.
인베딩 그리고 얘도 gru를 태울 거라서 gru 그리고 리니어 레이어가 필요해서 리니어도 하나 넣어줬습니다.
그래서 forward 함수에서 처음에 디코더의 input으로 들어가는 토큰이 있을 텐데 그거를 sos라는 start of sentence라는 토큰으로 채워줍니다.
그럼 이거를 기반으로 해서 문장이 시작이 되고 한마디로 이 start 토큰을 만들어주는 작업이라고 보면 돼요.
그래서 이거를 디코더의 입력으로 해서 내가 인코더에서 뽑은 히든 스테이트들 값들 적절히 조합을 해서 he라는 단어를 뱉게 되잖아요.
그래서 입력으로 들어가는 토큰을 생성해주는 게 이 디코더 input 부분 그래서 fill sos 토큰이라고 적혀있죠.
그리고 인코더 히든은 이 forward 함수에 들어오는 값이고 아웃풋 저장해줄 빈 리스트도 만들어줍니다.
그래서 디코더 부분에서는 이제 단어 하나씩 돌면서 내가 원하는 다음 단어를 생성해주는 게 반복되는 작업일 텐데
이 아래 보면은 target tensor가 non일 때랑 아닐 때가 나눠져 있어요.
그래서 target tensor가 non이라고 한다면 teacher forcing 방법으로 학습을 시켜주는 거라고 볼 수 있습니다.
이거는 정답, target이 되는 정답 데이터를 넣어주고 다음 토큰을 예측하도록 하는 방법이에요.
그래서 이 원래 RNM 모델 살펴보면 인코더로 들어가는 문장들이 있고 그거를 기반으로 아웃풋 단어를 하나씩 만들면서
그 생성된 단어가 다시 다음 디코더의 input으로 들어가는 구조잖아요.
근데 teacher forcing 방법으로 학습을 하면은 내가 어떤 단어를 예측을 하든지 다음 디코더로 들어가는 input에다가는 정답 값을 넣어준다라는 의미입니다.
그리고 밑에 else 부분에는 이제 teacher forcing이 아닌 경우인데 이거는 학습 과정에서 모델이 뱉은 결과 값을 활용해서 다음 디코더에 넣어준다.
라고 이해하시면 될 것 같아요.
그래서 그렇기 때문에 else 부분에서는 디코더의 output에서 top k 가장 확률이 높은 값을 가져와서 넣어주게 되는 거죠.
디코더의 input으로 그리고 여기 unskid 된 부분이나 skid 되는 부분들은 차원 맞춰주기 위한 건데
이렇게 제가 주석으로 어떻게 차원이 맞춰지는지는 참고용으로 적어뒀습니다.
그리고 이제 teacher forcing을 하지 않는 경우, 즉 모델의 output으로 나온 값을 가지고 다음 디코더의 input으로 넣는 경우에는 이 detach를 해주는 것을 볼 수가 있는데
이거는 이제 우리가 모델에서 나온 output을 가지고 다음 디코더의 입력으로 넣어주는 작업이기 때문에 그 output에는 gradient가 쭉 붙어 있겠죠.
그래서 그거가 input으로 들어갈 땐 필요가 없으니까 gradient를 끊어주는 게 이 detach라고 보시면 되겠습니다.
그래서 내가 지금까지 계산했던 gradient들은 필요 없고 그냥 나는 상수처럼 다음 디코더에 들어가는 값을 활용하겠다는 의미에서 detach를 해주신다고 보면 됩니다.
이거 해주는 이유는 이제 gradient 값까지 가져오게 되면 메모리를 아무래도 많이 사용하게 되니까 그거를 끊어주는 거라고 볼 수 있겠습니다.
그래서 이렇게 나온 output들 다 concat을 해주고 얘도 log softmax를 해주는데 이거는 이제 뒤에 우리가 나온 결과 값이랑 실제 정답 값을 비교해서 loss를 계산을 해줄 텐데
그 loss를, loss에도 종류가 여러 가지 있지만 저희가 사용할 loss는 log를 붙여줘야지 더 동작을 하는 loss라서 log softmax를 사용한 거라고 이해하시면 되겠습니다.
그리고 forward step도 적혀져 있어요.
그래서 embedding 태워주고 rel function, activation function 태워주고 그리고 gru, rnm 기반의 디코더 모델이 되는 값 뽑아서 output이랑 hidden이 나오면 그 중에 output을 output이라고 정의를 하고
그 다음에 hidden, 이 out은 지금 위에 올라가 보면은 linear layer죠. 그래서 linear layer 하나 더 태워서 output으로 만들어주고 return을 해주게 됩니다.
그렇게 하면은 지금 이 슬라이드에서 봤던 모델에 필요한 인코더와 디코더를 완성했다고 보면 돼요.
제가 처음에 시작할 때 그냥 sequence to sequence 모델이랑 attention이 적용된 sequence to sequence 모델 두 개로 둘 다 학습을 시켜볼 거라고 했는데 지금 완성한 인코더랑 디코더는 attention이 없는 sequence to sequence 모델이라고 보면 되고
아랫부분에서는 attention이 적용된 인코더와 디코더를 만들어주도록 하겠습니다.
그래서 attention을 적용시키기 위해서 일단은 attention이 적용되는 클래스를 하나 만들어줄게요. attention 아래는 attention, 디코더를 만들어주기 전에 attention 수식 계산을 해줄 수 있는 클래스를 하나 만들어주도록 하겠습니다.
여기서도 initialize 하는 부분에 학습이 되는 파라미터들을 정의해주는데 이거는 익숙한 구조죠.
그래서 저희가 맨 처음에 attention 연습을 해볼 때 지금 가중치 행렬에다가 제가 이미의 값지를 넣어주고 시작을 했다고 했는데 이 부분이 학습이 가능한 행렬이라고 말씀을 드렸었죠.
그래서 그 부분이 여기 이렇게 구현이 되어 있습니다. 그래서 linear 레이어를 만들어서 학습이 가능한 파라미터를 지정해주는 부분이라고 생각을 해주시면 되겠고
forward 부분에는 일단 처음에 score 계산하는 부분이 있었죠. 그래서 퀄이랑 키 가지고 attention score 자체를 계산을 해주고요.
그거를 softmax를 태워서 확률 합이 1이 되는 distribution으로 만들어서 weight로 만들어주게 됩니다.
그리고 이 weight랑 key를 가중합을 해서 최종 context를 생성을 해줘요.
그래서 이게 차원이 조금 헷갈릴 수가 있게 되는데 주석이 안 달려있어서 그러신 거죠.
그리고 제가 이거 주석 단 파일도 이따가 공유 드라이브에다가 업로드를 시켜주겠습니다.
시간이 조금 부족할 것 같아서 채워진 값으로 일단 설명을 하고 있기는 한데 밑에 부분에서는 그러면 한번 코드 작성해볼 시간을 드리도록 하겠습니다.
그래서 이 부분에서는 forward 함수 그러니까 attention을 적용시키기 위한 forward 함수들이라고 생각을 해주시면 되겠고
퀄이랑 key를 가지고 score를 계산을 해주고 그 score를 가지고 softmax를 태워서 weight 값을 만들어주고 weight 값이랑
context를 return을 해주는 그런 함수라고 보면 되겠습니다.
이제 이 attention을 기반으로 해서 이제 rnm 모델을 구현을 해봐야 하는데
이거는 그러면 쉬는 시간을 잠깐만 가지고 40분부터 다시 시작을 하도록 할게요.
그리고 제가 여기 투두를 좀 비워두도록 하겠습니다.
여기 팝 다이아몬드가 보이시나요?
네.
이게 지금 그 다른 리믹스의 각도 중에서 대단한 리믹스입니다.
자, 이게 정리를 따라서 이렇게 더 플레이 되는 거예요?
네.
이게 코드 세트예요?
네.
이번에 따라서 지금 투두를 가져와서 이렇게 반응하는 것 같아요.
여기부터 과연…
맵찍한 프로듀션을 가져와서 다음 이쁜 프로듀션을 만들려면
히든은 가져오는 게 아니라 앞으로 가져와서 가져와야 될 것 같아요.
첫 번째 이쁜 것 같이 가져와서 가져와야 될 것 같아요.
이거를 그냥 가져와야 될 것 같은데 이 히든을 가져와야 될 것 같아요.
사실 이거는 최종 이친 건 결갑값이라고 볼 수 있고
이걸 다시 모델을 활용하고 싶으시기 때문에
히든을 쓸 수 있는 결갑값으로 활용을 가져와서
그걸로 영상을 재생을 해줘야 진행돼서
실제 압도까지 아니라 그 압도까지 만들기 위해서 사용되었고
컨트롤까지 가져온던 것 같아요.
어떻게 보면 이 그림이 좀 숙성한 것 같아요.
그렇죠.
이건 저희가 일에서 여기서 호핑이 돼 있다가
뭐다? 하고 딱 나가면 뭐 이렇게 할 수 있는데
이렇게 대보적으로 들어가는 박대를
그리고 흘러가는 박대를
이 히든이 거의 바로 앞이 아니고
히든을 같이 가져와서 가져와야 될 것 같아요.
네.
말씀하신 것처럼 저희 항상 히든을 구해보니까
곡이 좀 더 타이핑한 것 같거든요.
아.
이런 느낌으로.
사실 이 부분은
하스팅을 보면서 준비를 했어서
시간이 좀 여전히 끝나야 되는데
그런 부분은 전혀 아무것도 없으고
그러면 이어서 작성을 해보도록 하겠습니다.
그래서 우리 방금 전에 attention 구현을 했고
이거는 사실 위에서
연습해봤던 거랑 동일한 메커니즘이다
라고 생각을 해주시면 될 것 같고
밑에서 이제 attention이 적용된
decoder rnn을 구현을 해뒀습니다.
여기서 사용되는 게 중요한 게
기존의 decoder랑의 차이점이 attention이 들어가 있다는 거
그래서 초기화 해줄 때에도
아까 위에서 선언해줬던 attention 클래스를 가져와가지고
학습될 파라미터 안에다가 넣어준 거라고 보면 되고
나머지 부분은 사실 그냥 기본 decoder랑 동일하다라고 볼 수 있습니다.
그리고 차이점이라고 한다면
여기 지금 grdu에 들어가는 값이 곱하기 2가 되어 있는 게
attention 스코어를 더해서 input으로 넣어주기 때문에
차원이 늘어난 거다라고 생각해주시면 되겠고
이제 forward 부분에서는
forward step을 계속 거치는 과정을 반복하는데
forward step은 아래쪽에 구현이 되어 있습니다.
그래서 이 embedding 값으로 만들어주는 부분은
아까 이전에 decoder랑 동일한 방법이고
dropout은 제가 아까 설명해 드렸지만
embedding vector 중에 일부를 랜덤하게 꺼서 학습에 적용을 함으로써
좀 더 regularization을 반영해서 학습을 하는 메커니즘이다
라고 이해해주시면 될 것 같고
이 부분이 살짝 헷갈릴 수 있는데
hidden으로 받아온 값을 query로 만들어주면서
permute를 해주는 작업이 있습니다.
permute가 그 차원의 순서를 바꿔주는 함수다라고 생각을 해주시면 되는데
decoder에서 hidden이 나올 때
layer 수, bet size, hidden size 이렇게 output으로 나오게 되는데
이거를 attention에 query로 넣어줄 때에는
bet size, 1, hidden size 이렇게 바꿔줘야 합니다.
그래서 우리가 기존에 나왔던 output 값을 가지고
attention query로 활용해주기 위해서
이렇게 shape를 바꿔줘야 되는데
그럼 bet size가 맨 앞으로 오게 되겠죠.
그래서 permute를 할 때
지금 이 순서가 0부터 시작해서 0, 1, 2라고 이해하시면 되는데
1번째 있는 게 맨 앞으로 오고
0번째 있는 게 두번째
그리고 두번째 있는 게 두번째
이렇게 바꿔서 적용을 한다는 의미로
permute를 사용한 거라고 볼 수 있습니다.
그래서 이거 코드 보시고도 느끼시겠지만
차원 맞춰주는 게 굉장히
딥러닝 모델 만들 때 중요하기 때문에
여러분들도 만약에 내가 딥러닝 모델을
처음부터 구축한다고 한다면
계속 그 프린트 되는 차원 값을 찍어보면서
permute나 skiz나 unskiz나
이런 식으로 차원 맞춰주는 함수들을 통해서
잘 맞춰가지고 모델을 완성하시면 좋을 것 같습니다.
그리고 그렇게 만들어진 커리를 가지고
attention 태워주는 작업을 해줍니다.
그래서 attention은 위에서
만들어준 클래스를 self attention으로
받아왔기 때문에
그거를 그대로 사용해서 attention을 계산을 하고
attention weight랑 context를 뽑아준다라고 볼 수 있고
우리가 만들어준 embedding에다가
이 context 값을 concat을 해서 concat은 이어붙이는 거죠.
그거를 해서 gru의 input으로 넣어주기 때문에
위에서 gru input에 곱하기 2를 해줬던 게
그런 이유라고 볼 수 있겠습니다.
그리고 self.gru에다가 input을 넣어서
값을 계산을 해주게 되고요.
그렇게 해서 나온 값이 output은 배치 사이즈 1
그리고 hidden 사이즈의 shape로 나오게 되고
hidden은 1, 배치 사이즈, hidden 사이즈의 값으로 나오게 됩니다.
이건 여러분들도 모델 돌리시면서 찍어보면
확인할 수 있는 부분이고
이렇게 나온 output은 다시 linear layer 하나를 통과시켜서
out의 linear layer라고 적어뒀죠.
그거를 통과시켜서 최종적인 output을 뽑아주게 되고
그냥 linear만 통과시킨 거니까
배치 사이즈 1 그리고 위에서 정의해줬던
output 사이즈에 맞춰서 값이 뽑아지게 됩니다.
그래서 최종적으로 decoder에서 return하는 값은
output과 hidden 그리고 attention weight을
return을 해주게 되고
사실 attention weight 같은 경우는 뒤에서 시각화
해주기 위해서 return한다고 생각해주시면 될 것 같습니다.
그래서 이 부분도 실행을 시켜줄게요.
그리고 나서 이렇게 지금 incoder랑 decoder가 준비가 됐고
attention을 적용시킨 decoder까지도 함수로 만들어 놨으니까
이제 트레이닝을 해볼 차례인데
트레이닝을 위해서 제가 import를
두 개를 해줬고 이것들은 중간중간
time 같은 거 찍어보기 위해서 import했다고 보시면 되고
여기 있는 함수들은 다들 이제
학습하는 과정에서 내가 지금
어느 단계까지 왔는지를 찍어보기 위해서
만들어둔 함수라고 이해하시면 될 것 같고
중요한 부분은 여기 data loader, get data loader 부분
여기서 이제 우리가 아까 텍스트 파일 읽어와서
pair를 만들어뒀잖아요. prepare data라는 함수를
만들어서 pair를 만들어뒀으니까 거기서 input이랑 output
그리고 pair data를 뽑아서
이렇게 input data랑
input IDs랑 target IDs 만들어주고
그거를 tensor의 data set에 넣어줘서 배치 단위로
차올 수 있는 형태로 만들어주게 됩니다.
이것도 지난 실습에서 써봤던 거랑 동일한 거라고 보면 되고
중요한 거는 이제 여기에 data 넣어줄 때
투 디바이스를 통해서 gpu에 올려진 상태
tensor의 형태로 만들어줘야 한다는 것을
참고를 해주시면 되겠고 그래서 이렇게 만든
트레이닝 데이터를 data loader 아까
위쪽에서 import 해왔던 거 있거든요. 데이터로더에 넣어서
트레이닝 데이터로더를 구성을 해주게 됩니다.
사실 조금 실습이 빨리
진행이 돼서 이 코드를 너무 헷갈리고 내가 완벽하게
작성을 못하겠어라고 생각하실 수도 있지만
지금 이거를 1부터 100까지 완벽하게 작성을 하는 거를 목표로
하시기 보다는 이거를 이해할 수 있는 수준
정도로 목표를 잡으시면 좋을 것 같습니다.
그래서 트레이닝 위에서 필요했던 데이터로더
가져오는 작업을 해주는 함수를 실행을 시켜주고
이제 우리가 핵심적으로 잘 알아야 되는 부분이
이 train epoch를 해주는 부분이죠.
그래서 이거에 제가 투드들을 넣어뒀어요.
데이터로더 위에서 만들었던 거에서 배치 단위로 데이터들을 가져오게 될 거고
그거를 인프텐서와 타겟텐서로 제정을 해줄 텐데
이것들을 활용해서 지금 쭉 진행되는 부분에
적힌 투드를 채워보면 좋을 것 같습니다.
처음에 그래디언트 초기화 해주는 부분, 데이터로더가
시작할 때 그래디언트 초기화 해줘야 한다고 했었잖아요.
그 부분이 있고 그 다음에 인코더, 디코더를
태우는 부분은 지금 작성이 되어 있고
로스를 계산하는 부분이 있습니다.
로스를 계산하는 부분에서 이 부분 채워주시면 될 것 같고
로스 계산하면 해야 될 게 있죠. 우리 모델 업데이트 해주는 거
그래디언트 계산해주는 거 그 부분을 이 아래에 채워주시면 되겠습니다.
조금 시간이 촉박해서
한 5분 정도 시간을 드리고
이어서 진행해보도록 할게요.
여기 로스 부분에 투드 채우실 때는
제가 여기 화면에 보이는 주석으로 적어둔
이 쉐입을 맞추는 작업을 투드해서 채워주시면 되거든요.
그래서 이거 참고해서 작성해보시면 되겠습니다.
자, 이제 마지막으로
로스를 계산하는 부분에
네, 그러면 같이 작성을 해보도록 할게요.
일단 데이터 로더에서 데이터 배치 단위로
뽑아와서 그거를 인톡과 타겟, 정답으로
나눠서 담아줬습니다, 변수에.
그리고 제가 여기 그래디언트 초기화라고 적어뒀는데
그래디언트 초기화라는 건 옵티마이저에서 계속 저장해두고 있었던
그런 이전 스텝의 그래디언트들을 0으로 초기화를 시켜줘야 된다는 의미인데
지금 트레인 에폭으로 들어오는 함수 살펴보면
인코더 옵티마이저랑 디코더 옵티마이저 두 개가
존재하는 걸 알 수 있습니다.
그래서 우리는 그 두 개의 옵티마이저의 그래디언트를 둘 다
초기화시켜주면 된다고 생각하시면 되겠고
처음에 인코더 옵티마이저에서 0그래드 적용을 해서
초기화를 시켜주고
디코더 옵티마이저도 마찬가지로 초기화를 시켜주면 되겠습니다.
그리고 트레인 에폭으로 들어온 인코더랑 디코더
저희가 이전 스텝에서 RNN 인코더, RNN 디코더, 그리고
텐션이 포함된 디코더 등등을 만들어줬었는데
그 값들이 트레인 에폭의 파라미터들로 전달이 되면
그거를 사용해서 인프텐서를 받아와서 인코더 아웃풋을 만들고
그거를 기반으로 디코더에 태워서 디코더 아웃풋도 만들어주게 됩니다.
이렇게 짝대기로 표시된 것은 활용하지 않을 값이라서 이렇게 적혀 있고
디코더 아웃풋만 나는 활용할 거라고 코드를 적어준 거라고 이해하시면 될 것 같고
다음에 디코더에서 아웃풋까지 뽑았으면
우리가 정답에 있었던 타겟, 아까 위에서 이 텐서 타겟을 저장을 해줬었는데
이 두 개를 비교를 해서 로스를 계산을 해야겠죠.
로스를 계산을 해야지 그래디언트를 계산할 수 있고 모델을 업데이트 해줄 수가 있으니까
이 부분에서 로스를 계산을 해주는 건데
이것도 지금 이 함수에 전달되는 값입니다.
그래서 어떤 로스 함수를 사용할 건지는 이 함수를 호출하는 부분에서 전달해주면 되는 부분이고
우리는 이 내부에다가 디코더의 아웃풋 그리고 정답 값, 투드에 적어줄 정답 값을 넣어서 로스 함수로 전달을 해주면
둘 사이에 로스를 계산해서 값이 반환이 되게 되는 거겠죠.
디코더 아웃풋을 살펴보면 여기 뷰가 적혀져 있어요.
그래서 쉐입을 좀 바꿔준 거죠.
바꾼 쉐입을 어떻게 했는지 살펴보면 디코더 아웃풋에서 맨 마지막 값, 사이즈 맨 마지막 값을 가져왔다는 게
보케블러리의 사이즈가 될 거거든요.
왜냐하면 디코더는 어쨌든 최종적으로 내가 다음 스텝에서 예측하고 싶은 단어의 확률분포를 뱉는 구조로 저희가 설계를 해줬는데
그 확률을 어떤 단어들에서 계산을 할 건지를 살펴보면 내가 전체 후보가 되는 보케블러리가 있을 거잖아요.
그러니까 후보가 되는 단어들 전체를 가지고 확률을 계산을 하게 되고 거기서 가장 높은 단어,
그러니까 보켑 내부에 있는 가장 높은 확률을 가진 단어를 최종 아웃풋으로 출력하게 되는 거라서
이 디코더 아웃풋의 맨 마지막 차원의 사이즈를 가져오게 된다는 거는 보케블러리의 사이즈를 가져오게 된다는 거랑 같은 의미입니다.
그래서 그거를 지금 뷰의 두 번째 인자로 줬고 첫 번째 인자를 마이너스 1로 주면 두 번째 인자에 맞춰져서 첫 번째 쉐입이 결정이 되는 거죠.
그래서 그 작업을 거치면 최종 출력이 제가 적어둔 것처럼 배치 사이즈 곱하기 시퀀스 랭스랑 보켑 사이즈로 이루어진 쉐입으로 바뀌게 됩니다.
그래서 원래는 이제 배치 사이즈, 시퀀스 랭스, 보켑 사이즈 이렇게 구성이 되어 있었을 텐데 뷰를 통해서 앞에 두 개를 합해준 거라고 이해를 하시면 되겠고
그러면 투드에서 내가 정답으로 넣어줘야 하는 타겟 값도 원래는 배치 사이즈랑 시퀀스 랭스로 이루어진 쉐입을 가지고 있습니다.
근데 이거를 내가 지금 아웃풋을 바꿔줬죠 쉐입을.
그래서 이것도 이런 쉐입으로 바꿔줘야지 두 개가 비교가 될 수 있기 때문에 그렇게 바꾸는 작업을 해주면 되겠습니다.
그래서 타겟 텐서 있죠? 아까 위쪽에서 타겟 텐서라고 적용을 시켜줬고 여기서 뷔가 이제 넌파이의 리쉐입과 같은 역할을 하는 파이토치의 기능이라고 첫 번째 시간에 소개를 드렸었는데
여기서 마이너스 1을 적용을 해서 위에 적혀 있듯이 이렇게 되어 있던 거를 이 쉐입으로 변경을 시켜주도록 하겠습니다.
그리고 이거를 로스 함수에 전달을 해서 최종적인 로스를 뽑아주고 로스를 뽑았으면 이제 해줘야 되는 일들이 정해져 있죠.
로스 점 백워드 함수 호출을 해서 그래디언트들을 쭉 계산을 해주고 이제 그거를 기반으로 모델을 업데이트 시켜줘야 되는데
우리가 지금 위에서 초기화할 때도 알 수 있었듯이 지금 옵티마이저가 두 개 존재해요.
그래서 둘 다 업데이트를 시켜주면 됩니다.
그래서 인코더 옵티마이저 점 스텝 한번 해주고 그 다음에 디코더 옵티마이저 점 스텝 한번 해주면 되고
이제 토탈로스는 더해져서 다시 다음 배치로 넘어가게 되겠죠.
이런 식으로 트레인 에폭을 구성을 해줄 수가 있습니다.
이 셀도 실행을 해주시고 다음으로는 이 트레인 에폭 만들어준 함수를 기반으로 전체 트레인 과정을 총괄하는 그런 함수를 또 만들어주면 되는데요.
일단 위쪽에 지금 정의되어 있는 부분은 트레인 시간을 측정하기 위한 값 그리고 플러팅 시각화 해주기 위한 값들이 적혀져 있다고 보면 되고
여기서 우리가 옵티마이저랑 로스 함수를 설정을 해줘야 합니다.
그래서 이 부분 제가 투드라고 적혀져 있는 부분에다가 옵티마이저를 만들어주시면 되겠고
저희가 자주 사용했던 그 옵티마이저 있죠.
그거를 활용해서 만들어주시면 되겠고
로스는 지금 NLL로스라는 게 설정이 되어 있습니다.
이거는 negative log likelihood loss라는 로스 계산하는 함수인데
저희가 아까 위에서 디코더 출력을 logsoftmax를 적용을 시켜줬던 이유가 이 로스를 활용하기 위해서라고 이해를 해주시면 되겠습니다.
그래서 이 옵티마이저 부분 작성해볼게요.
네, 그래서 우리 위쪽에서 이제 트레인 에폭 함수 정의할 때
인코더 옵티마이저랑 디코더 옵티마이저 이렇게 두 개씩 사용을 해줬었잖아요.
그래서 트레인 부분에서도 옵티마이저가 두 개가 필요하다는 것을 알 수가 있고
첫 번째로 인코더 옵티마이저부터 만들어주도록 할게요.
이거는 저희가 계속 써왔던 아담이라는 옵티마이저를 사용해 줄 거고
내부에다가는 이 옵티마이저를 통해서 학습이 되었으면 하는 그런 파라미터들을 전달해 주면 되는데
인코더 옵티마이저니까 인코더에 관련한 파라미터들을 넣어주면 되겠죠.
그래서 인코더.파라미터를 이렇게 적어 주겠습니다.
그리고 옵티마이저에 전달되어야 될 것 중에 하나가
러닝레이트를 전달을 해줘야 되는데
러닝레이트는 지금 트레인 함수의 파라미터 값으로 입력을 받고 있어요.
그래서 이거를 그대로 사용해 주도록 하겠습니다.
그리고 디코더 옵티마이저도 구현을 해주면 되는데
얘도 아담 옵티마이저를 사용을 해주고
이번에는 전달해야 될 파라미터가 디코더에 있는 파라미터들을 전달을 해줘야겠죠.
그래서 이렇게 전달해 주고
러닝레이트는 동일한 러닝레이트를 사용을 해 주겠습니다.
이렇게 하고 이제 로스 함수를 저장해 줬고
이 에폭이 돌아갈 때 위에서 저희가 만들어줬던 트레인 에폭이라는 함수를 호출을 하면서
위에 작성됐던 작업들을 반복을 하게 되요.
그래서 옵티마이저 초기화 해주고
아웃폿들을 뱉어서 로스 계산하고
백워드해서 업데이트 해주는
일련의 과정이 여기 안에 들어있다고 생각을 해주시면 되겠습니다.
그래서 이 부분에서는 이제 배치 단위로
그렇게 학습을 진행시키면서
결과 값들을 출력해주는 부분이라고 볼 수 있을 것 같아요.
여기 함수도 실행을 해보고
트레인하는 함수까지 만들어줬으면
여기는 이제 쇼플랏이라는 위쪽에서 사용되고 있는 요고 있죠.
요고를 구현을 해준 거라고 보면 되고
시각화를 위한 도구라고 볼 수 있습니다.
그리고 이제 중요한 건 아랫부분에 있는 이 앱 이벨리웨이트 하는 거
그래서 우리가 학습을 시켜줬으면
그 학습된 파라미터들의 최종적인 웨이트들이 결정이 될 텐데
그거를 가지고 학습이 잘 되었는지를 평가하는 부분이 필요하겠죠.
그래서 이벨리웨이트라는 함수를 구현을 해볼 건데
여기서 제가 윗 옆에 투드라고 적어놨어요.
간단한 코드만 작성하면 되는 건데
같이 한번 적어보도록 하겠습니다.
그래서 이벨리웨이트를 할 때 중요한 게
트레인이랑 바뀌는 점이
트레인할 때는 우리가 그라디언트를 계산해서
얘를 업데이트해주는 작업을 반복하는 부분이었잖아요.
근데 이벨리웨이트를 할 때는 그라디언트를 계산해줄 필요가 없습니다.
그래서 이벨리웨이트를 시작할 때는 항상 위드로 감싸진 부분에다가
토치점, 노그래드를 선언을 해줍니다.
그래서 여기가 그라디언트를 계산하지 않겠다.
왜냐하면 이제 이벨리웨이트를 한다고 해서
다른 인코더, 다른 디코더를 쓰는 게 아니라
우리가 위에서 정의해줬던 그런 인코더, 디코더를
사용을 해주는 거기 때문에
같은 인코더, 디코더를 사용하지만
그라디언트 계산은 빼고 하겠다고 선언을 해주는 겁니다.
그래서 이벨리웨이트를 할 때도 인프텐서로 만들어주고
입력 문장을 그거를 인코더에 태우고
그 결과 값을 디코더에 태우고
디코더 아웃풋에서 가장 확률이 높았던 값을 가져와서
불필요한 처원들은 스키즈로 제거를 해준 다음에
이 디코더 워즈에 다 담아주게 됩니다.
그래서 이거를 언제까지 반복하냐?
워즈에 담아주는 거는 문장을 계속 한 단어씩 뱉을 텐데
모델이 EOS 토큰을 뱉을 때가 있을 거예요.
그때가 이제 엔드 오브 시퀀스라는 토큰을 뱉는 건데
이게 나오면 문장이 종료된 거다라고 이해를 하시면 되겠습니다.
학습을 할 때도 문장의 끝 뒤에는 EOS 토큰을 뱉도록 학습이 되어 있기 때문에
실제로 모델을 사용할 때도 이 문장이 나오면 종료되었구나라고 생각을 해주시면 될 것 같아요.
그래서 이 함수도 실행을 해주겠습니다.
그리고 아랫부분에는 제가 학습이 다 된 다음에
이 벨루엣을 할 때 좀 랜덤하게 데이터에서 샘플들을 가져와서
실제로 문장들이 어떻게 지금 이 모델을 통해 어떻게 번역이 되고 있는지를 보려고
이 함수를 만들어둔 거라고 이해하시면 될 것 같고
이 페어에서 랜덤하게 초이스를 해서 한 쌍을 가져오고
그거를 출력을 해주는 겁니다.
그래서 첫 번째로 페어의 0번째니까 input이 되는 문장 출력해주고
정답이 되는 문장, 페어의 첫 번째 값을 가져와서 정답이 되는 값을 출력을 해주고
그리고 우리가 예측된 값은 이 벨루엣이라는 위에 만들어뒀던 거 있죠?
이 함수를 거쳐서 input 문장을 넣어준 결과 값을
이제 아프 센텐스에 단어들을 쭉 엮어서 출력을 한다고 보시면 되겠습니다.
이 함수도 실행을 해줄게요.
그리고 나서 이 부분이 저희가 지금까지 정의해줬던 많은 함수들 있죠?
그것들을 사용해서 실제로 학습을 돌리는 부분이라고 보면 되는데
위쪽에서 계속 변수로 두고 사용했던 게 이제 히든 사이즈랑 배치 사이즈 등이 있습니다.
실제로 학습을 돌릴 때는 내가 히든 사이즈를 몇으로 해줄지 그리고 배치 사이즈
이거는 데이터를 얼마나 쪼개서 볼지 그 뭉치의 단위라고 말씀드렸었는데
그거를 몇으로 볼지를 지정을 해주면 되겠고
지금은 128과 32로 정의를 해주겠습니다.
그리고 데이터 로더에 배치 사이즈를 전달을 해서 이 32 단위로 값들을 가져온다 라고 볼 수 있겠고
인코더 RNN과 어텐션 디코더 RNN 저희 만들어뒀었죠.
그거를 통과시킨 결과 값들을 뽑아주겠습니다.
그리고 트레인에 넣어주도록 할게요.
제가 말 잘못했는데 여기서 통과시킨 결과 값을 뽑는 게 아니라 모델들을 올려주는 거라고 볼 수 있겠습니다.
인코더 모델을 어텐션 인코더라는 변수에 담아준 거고
어텐션 디코더 RNN을 어텐션 디코더라는 변수에 담아준 거라고 볼 수 있겠고
모델들이기 때문에 투 디바이스에서 GPU 단위에 올려서
이 저장된 모델들을 트레인 함수에다가 파라미터로 전달을 시켜줄게요.
그리고 N 에폭은 그때 첫 시간에 말씀드렸던 것 같은데
이 데이터 전체를 총 몇 번 볼 건지를 정해주는 파라미터라고 말씀드렸었습니다.
저희가 지금 시간이 없으니까 E로 해서 한번 시행을 시켜보도록 할게요.
이 코드를 실행을 해보시면 위에 적어뒀던 게 반영이 되면서 쭉 학습이 진행이 되게 됩니다.
그래서 지금 두 번 에폭만 돌면 되기 때문에 두 개의 결과 값이 찍히고
코드가 종료된 것을 확인해볼 수가 있고
이렇게 만들어진 모델을 가지고 평가를 해볼 수가 있다고 했죠.
그래서 평가를 위해서 아까 정의해뒀던 이 함수를 호출해서 랜덤한 문장 몇 개를 넣어보도록 하겠습니다.
만약에 이런 프랑스 문장이 들어왔을 때 정답이 이렇게 되는데
저희가 지금 에폭 두 번 돌려서 학습된 모델은 이런 답을 뱉어냈다고 보시면 되겠습니다.
아직 학습을 두 번 밖에 안 돌려서 좀 잘 못하죠.
지금 정답이 이건데 출력이 이렇게 나온 거니까.
그리고 이 아래에 있는 부분에다가는 이제 어텐션이 없는 그냥 디코더 RNN을 모델로 넣어서 또 학습을 돌려보겠습니다.
이것도 지금 에폭을 2로 설정을 해줘서 로스 두 개가 찍힌 거 볼 수 있고
이것도 실행을 해보면 지금 결과 값을 확인해볼 수 있는데
사실 위에 저희가 어텐션 적용한 그 모델도 학습을 조금밖에 안 돌려서 잘 못했었고
얘도 지금 학습을 조금밖에 안 돌려서 지금 둘이 비교가 어렵긴 한데
어쨌든 만들어둔 모델로 이런 문장을 생성할 수 있는 기계 번역 모델을 만든 거다라고 이해를 하시면 되겠고
이거를 조금 시각화를 하기 위해서 어텐션 시각화하는 함수들을 가져왔는데
이제 이런 문장들이 입력된다고 했을 때 어떻게 어텐션이 걸리는지
1시각화를 해보면 지금 학습이 아직 충분히 되지는 않았지만
그래도 이 대각선 방향으로 조금 더 밝은 그러니까 어텐션이 이런 순서로 걸렸다는 것을 확인을 해볼 수가 있습니다.
그래서 내가 i라는 단어를 예측을 할 때 지금 참고한 단어들을 보면은 여기 위주로 참고했다라는 걸 알 수 있고
그 다음에 아임, 이 m을 예측할 때는 여기, 소리를 예측할 때는 여기
점점 이제 뒤쪽 문장을, 뒤쪽 단어들을 참고하면서 만들었구나라고 이해하시면 되겠습니다.
그래서 지금 이제 시간이 또 점심시간이 다 되가는데
이거를 저희가 에폭을 2로 학습을 했었잖아요.
근데 이거를 한 100이나 200 정도로 돌려놓고 이제 식사하고 오시면 아마 모델이 돌아가 있을 것 같거든요.
그래서 그렇게 돌려두시고 이벌리웨이트 하는 거를 해보시면
확실히 이제 어텐션을 적용한 모델이 더 좋은 결과를 만드는 거를 확인을 해보실 수가 있습니다.
그래서 저희 이게 조금 시간이 오래 걸려서 얘랑 이 코드 그리고 이 기본 RNN 사용했던 코드 이렇게 4개 돌려두시면
이제 식사하고 오셔서 확인을 해보실 수 있을 것 같고
그리고 사실 제가 뒤에 트랜스포머, 저희가 지금까지 해본 건 트랜스포머가 아니고
어텐션이 적용된 시퀀스트 시퀀스 모델을 만들어본 거고
이제 트랜스포머 또 만들어보려고 했는데
사실 실습이 조금 어렵다고 하시는 분들도 계셨고
그리고 조금 더 천천히 하는 게 좋을 것 같아서
이 부분은 실습을 진행을 하지는 못할 것 같은데
제가 여기다가 그래서 주석을 좀 꼼꼼하게 달아뒀거든요.
그래서 이거를 읽고 참고를 해보시면 좋을 것 같습니다.
그래서 사실 구현해야 되는 거는 셀프 어텐션 부분 그러니까 위쪽에서 했던 거랑 동일해요.
어떤 X 값을 입력을 받아서 그거를 가지고 같은 문장 내에서 어텐션을 주고 받는 거
그리고 그거를 기반으로 해서 멀티 헤드 어텐션을 구현을 해둔 부분이 있습니다.
멀티 헤드 어텐션은 제가 여러 개의 헤드를 가진 어텐션 메커니즘을 구현을 한 거라고
아까 슬라이드에서 말씀을 드렸었죠.
그래서 이게 어떻게 구현되는지 코드로 살펴보시면 될 것 같고
그리고 풀 아키텍쳐도 넣어줬어요.
그래서 여기에서 이 구조, 이 트랜스포머의 풀 구조를 만들기 위해서 어떻게 해야 되는지
인코더, 디코더 아키텍쳐 구현된 부분 그리고 제너레이터 구현된 부분 그리고 인코더 스택 쌓는 부분
그리고 이제 그 아키텍쳐 보면 중간중간에 레이어 노멜라이제이션이랑 레지듀얼 커넥트를 구현하는 부분이 있는데
그것들도 코드로 어떻게 구현하는지를 제가 다 주석을 적어뒀습니다.
그래서 이 부분은 한번 참고용으로 읽어보시면 될 것 같고
실제로 만약에 내가 나중에 모델을 구현해서 어떤 모델을 만들고 싶으면
저희가 실습했던 것처럼 하나씩 다 입력해서 모델을 만드는 게 아니라
기터브에 잘 공개되어 있는 그런 여러 연구자들이 만들어둔 모델을 사용하는 경우가 거의 99.9%일 것이기 때문에
코드를 처음부터 작성을 해야지라고 생각하는 것보다
이 코드를 보고 이 부분이 인코더에 어텐션을 적용한 부분은
이 부분은 디코더에서 출력하는 부분이구나 이런 정도를 이해하는 것을 목표로 두고 하시면 좋을 것 같습니다.
오늘 오전 실습은 여기까지 하도록 하고
이따가 돌려두신 모델 결과가 나오면
내가 만든 어텐션이 적용된 시커드가 만들어졌습니다.
수고하셨습니다.