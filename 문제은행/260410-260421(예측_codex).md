# 딥러닝 기초 시험 예측 문제 (260410~260421)

> 작성 기준: `문제은행/260410-260421.md`와 `260410`~`260421` 폴더의 원본 세션 파일만 참고함.
>
> 제외한 파일: `문제은행/260410-260421(예상).md`, 각 폴더의 `*요약.md` 파일.

# 1. 기존 12문제 출처 분석

## 1.1 문제별 세션 매핑

| 번호 | 기존 문제 | 핵심 주제 | 출처 세션 추정 | 근거 |
|---:|---|---|---|---|
| Q1 | 지도 학습과 비지도 학습 차이 | ML 학습 방식 | `260410 오전/오후` | supervised/unsupervised 학습 개요 |
| Q2 | 과적합 | generalization, noise fitting | `260410 오전/오후` | overfitting과 regularization 필요성 |
| Q3 | Gradient Descent 원리 | loss 최소화, gradient update | `260410 오전` | 최적화 기본 원리 |
| Q4 | L1/L2 정규화 | norm penalty, sparse/weight decay | `260410 오후` | regularization |
| Q5 | 활성화 함수 필요성 | non-linearity | `260413 오전` | MLP와 activation function |
| Q6 | Backpropagation 역할 | chain rule, gradient 계산 | `260413 오후` | NumPy/PyTorch backprop 실습 |
| Q7 | Convolution이 이미지 처리에 적합한 이유 | locality, weight sharing | `260414 오전/오후` | CNN fundamentals |
| Q8 | Pooling 역할 | dimension reduction, robustness | `260414 오전/오후` | CNN pooling layer |
| Q9 | RNN과 MLP의 구조 차이 | hidden state, sequence | `260415 오전` | RNN 기초 |
| Q10 | LSTM이 해결한 RNN 문제 | vanishing gradient, long-term dependency | `260415 오후` | LSTM/GRU |
| Q11 | Transformer Attention 역할 | token dependency, weighted sum | `260416 오전/오후`, `260417 오후` | self-attention/cross-attention |
| Q12 | Positional Encoding 필요성 | token order | `260416 오전/오후`, `260417 오후` | Transformer position 정보 |

## 1.2 세션별 커버 현황

| 세션 | 기존 문제 반영 정도 | 남은 고확률 주제 |
|---|---:|---|
| `260410 오전` | 반영됨 | optimizer 세부: SGD, momentum, Adam |
| `260410 오후` | 반영됨 | regularization의 넓은 의미, dropout |
| `260413 오전` | 반영됨 | activation function별 특징, vanishing gradient |
| `260413 오후` | 반영됨 | batch normalization, residual connection 기초 |
| `260414 오전` | 반영됨 | CNN output size, ImageNet/AlexNet/ResNet |
| `260414 오후` | 일부 반영 | data augmentation, BatchNorm, Dropout, Residual connection |
| `260415 오전` | 반영됨 | RNN mapping types, parameter sharing |
| `260415 오후` | 일부 반영 | bidirectional/deep RNN, exploding gradient, GRU |
| `260416 오전` | 반영됨 | self-attention vs cross-attention, teacher forcing |
| `260416 오후` | 일부 반영 | modern Transformer block, GQA/MLA, MoE |
| `260417 오전` | 거의 미반영 | generative modeling, latent variable, VAE intro |
| `260417 오후` | 거의 미반영 | ELBO, KL divergence, VAE objective |
| `260420 오전` | 거의 미반영 | VAE loss, reparameterization, posterior collapse |
| `260420 오후` | 거의 미반영 | VAE implementation, reconstruction loss, KL regularizer |
| `260421 오전` | 미반영 | GAN, generator/discriminator, minimax, mode collapse |
| `260421 오후` | 미반영 | conditional GAN, FID, GAN vs VAE |

기존 12문제는 딥러닝 기본 구조까지는 넓게 커버하지만, 뒤쪽의 생성형 모델 파트가 거의 비어 있다. 숨겨진 4문제는 `260417`~`260421`의 VAE/GAN 쪽에서 많이 나올 가능성이 높고, 일부는 `260414~260416`의 학습 테크닉/Transformer 세부 구조에서 나올 수 있다.

# 2. 기존 문제 스타일 분석

- **대부분 개념 서술형**이다. 계산을 요구하기보다 "무엇이며 왜 필요한가"를 묻는다.
- **문제 길이가 짧다.** 한 문장 질문이 많고, 답안도 핵심 개념 2~4문장 정도다.
- **용어 비교형**이 많다. 지도/비지도, L1/L2, RNN/MLP처럼 차이를 묻는다.
- **역할/필요성 설명형**이 많다. 활성화 함수, pooling, attention, positional encoding이 왜 필요한지 묻는다.
- **강의의 대표 키워드 중심**이다. 세부 코드 암기보다 구조와 직관을 묻는다.

따라서 예측 문제도 복잡한 수식 증명보다 `역할`, `차이`, `왜 필요한가`, `어떤 문제를 해결하는가` 형식으로 구성한다.

# 3. 숨겨진 4문제 유력 범위

가장 유력한 파트는 다음 순서다.

1. **VAE**: Autoencoder와 VAE 차이, latent variable, reconstruction loss, KL regularization, reparameterization trick
2. **GAN**: Generator/Discriminator, minimax game, GAN 장단점, mode collapse
3. **CNN training techniques**: data augmentation, BatchNorm, Dropout, Residual connection
4. **Transformer 세부 구조**: self-attention vs cross-attention, masked self-attention, modern Transformer block
5. **RNN 세부 문제**: exploding gradient, bidirectional RNN, GRU

실제 숨겨진 4문제를 찍는다면 가장 가능성 높은 조합은 다음이다.

```
1문제: VAE 기본 구조 또는 VAE loss
1문제: GAN 기본 구조 또는 minimax objective
1문제: GAN/VAE 비교 또는 mode collapse
1문제: BatchNorm/Dropout/Residual 또는 Transformer attention 세부 비교
```

---

# 4. 고확률 예측 문제 8개

## 예측문제 1. Autoencoder와 VAE의 차이

Autoencoder와 Variational Autoencoder(VAE)의 차이를 latent variable 관점에서 설명하시오.

> **모범 답안**
>
> Autoencoder는 입력 `x`를 encoder로 압축해 latent representation `h`를 만들고, decoder로 다시 `x`를 복원하도록 학습한다. 하지만 일반 Autoencoder의 latent space는 우리가 아는 단순한 분포로 정리되어 있지 않아, 임의로 샘플링해서 새로운 데이터를 생성하기 어렵다.
>
> VAE는 encoder가 하나의 벡터가 아니라 latent distribution의 파라미터, 예를 들어 `mu`와 `sigma`를 출력하게 한다. 그리고 KL divergence regularization을 통해 latent distribution이 표준정규분포 같은 prior에서 크게 벗어나지 않도록 묶는다. 그래서 학습 후에는 prior에서 `z`를 샘플링해 decoder에 넣어 새로운 데이터를 생성할 수 있다.

채점 포인트

```
● Autoencoder = encoding/decoding으로 reconstruction
● VAE = latent distribution을 학습
● prior distribution에 맞추는 KL regularization 언급
● VAE는 sampling을 통한 generation 가능
```

---

## 예측문제 2. VAE Loss의 두 구성 요소

VAE의 loss는 크게 Reconstruction Loss와 KL Divergence 항으로 구성된다. 두 항의 역할을 각각 설명하시오.

> **모범 답안**
>
> Reconstruction Loss는 encoder와 decoder를 거친 출력이 원래 입력과 얼마나 비슷한지를 측정한다. 즉 입력을 잘 복원하도록 decoder를 학습시키는 항이다. 연속 데이터에서는 MSE, 이진 데이터에서는 Binary Cross Entropy, categorical 데이터에서는 Cross Entropy를 사용할 수 있다.
>
> KL Divergence 항은 encoder가 만든 latent distribution `q(z|x)`가 우리가 정한 prior distribution `p(z)`, 보통 표준정규분포에서 너무 멀어지지 않도록 제한하는 regularizer 역할을 한다. 이 항 덕분에 latent space가 샘플링 가능한 형태로 정리되고, VAE가 생성 모델로 동작할 수 있다.

채점 포인트

```
● Reconstruction Loss = 입력 복원 품질
● KL Divergence = q(z|x)를 p(z)에 가깝게 함
● KL 항이 regularizer 역할을 한다는 설명
● 데이터 타입에 따라 reconstruction loss가 달라질 수 있음
```

---

## 예측문제 3. Reparameterization Trick

VAE에서 Reparameterization Trick이 필요한 이유를 설명하시오.

> **모범 답안**
>
> VAE는 encoder가 출력한 분포에서 latent variable `z`를 샘플링한 뒤 decoder에 넣는다. 그런데 단순한 random sampling은 미분 가능하지 않아 backpropagation으로 encoder까지 gradient를 전달하기 어렵다.
>
> Reparameterization Trick은 `z`를 직접 샘플링하지 않고, `epsilon ~ N(0, I)`를 샘플링한 뒤 `z = mu + sigma * epsilon`처럼 표현한다. 이렇게 하면 randomness는 `epsilon`으로 분리되고, `mu`와 `sigma`에 대해서는 미분 가능해져 VAE 전체를 gradient descent로 학습할 수 있다.

채점 포인트

```
● sampling은 미분 불가능하다는 문제
● z = mu + sigma * epsilon 형태
● randomness와 학습 파라미터 분리
● backpropagation 가능하게 함
```

---

## 예측문제 4. GAN의 Generator와 Discriminator

GAN(Generative Adversarial Network)에서 Generator와 Discriminator의 역할을 각각 설명하시오.

> **모범 답안**
>
> Generator는 단순한 noise vector `z`를 입력받아 실제 데이터처럼 보이는 fake sample을 생성하는 네트워크이다. 목표는 Discriminator를 속일 만큼 그럴듯한 데이터를 만드는 것이다.
>
> Discriminator는 입력 sample이 real data에서 온 것인지, Generator가 만든 fake data인지 구분하는 네트워크이다. 목표는 real sample은 real로, fake sample은 fake로 정확히 분류하는 것이다.
>
> GAN은 이 두 네트워크가 서로 경쟁하는 two-player minimax game으로 학습된다. Discriminator가 좋아질수록 Generator는 더 진짜 같은 sample을 만들도록 학습된다.

채점 포인트

```
● Generator = noise z에서 fake sample 생성
● Discriminator = real/fake 판별
● 두 모델이 적대적으로 학습
● minimax game 또는 game theory 언급
```

---

## 예측문제 5. GAN의 Minimax Objective와 Mode Collapse

GAN 학습이 왜 불안정할 수 있는지 설명하고, Mode Collapse가 무엇인지 서술하시오.

> **모범 답안**
>
> GAN은 Generator와 Discriminator가 서로 반대 목적을 가지고 번갈아 학습하는 구조이다. Discriminator는 진짜와 가짜를 잘 구분하려 하고, Generator는 Discriminator를 속이려 한다. 두 네트워크의 균형이 맞지 않으면 학습이 발산하거나 한쪽이 너무 강해져 gradient가 제대로 전달되지 않을 수 있다.
>
> Mode Collapse는 Generator가 다양한 sample을 만들지 못하고 비슷한 sample만 반복해서 생성하는 현상이다. 예를 들어 여러 종류의 이미지를 만들어야 하는데 특정 클래스나 특정 형태의 이미지만 계속 만드는 경우이다. GAN은 sharp한 sample을 잘 만들 수 있지만, 초기 GAN에서는 diversity가 떨어지는 문제가 자주 발생했다.

채점 포인트

```
● Generator와 Discriminator의 경쟁 구조
● 학습 균형이 깨지면 unstable
● Mode Collapse = 비슷한 sample만 반복 생성
● quality-diversity trade-off 언급 가능
```

---

## 예측문제 6. VAE와 GAN의 장단점 비교

VAE와 GAN을 생성 품질과 다양성 관점에서 비교하시오.

> **모범 답안**
>
> VAE는 latent distribution을 prior에 맞추며 학습하므로 latent space가 비교적 안정적이고 다양한 sample을 생성하기 쉽다. 하지만 초기 VAE는 reconstruction 중심 학습 때문에 생성 이미지가 흐릿해지는(blurry) 문제가 있었다.
>
> GAN은 Discriminator와 경쟁하면서 학습하므로 상대적으로 선명하고 현실적인 sample을 만들 수 있다. 하지만 학습이 불안정하고, Generator가 특정 mode에만 몰리는 Mode Collapse로 인해 sample diversity가 떨어질 수 있다.
>
> 요약하면 VAE는 diversity와 안정성 쪽에 강점이 있고, GAN은 sample quality 쪽에 강점이 있지만 training stability와 diversity 문제가 있다.

채점 포인트

```
● VAE = 안정적, 다양한 sample, blurry 가능
● GAN = sharp/high-quality sample
● GAN = unstable training, mode collapse 가능
● quality-diversity trade-off 설명
```

---

## 예측문제 7. Batch Normalization과 Dropout

Batch Normalization과 Dropout의 역할을 각각 설명하고, training mode와 evaluation mode에서 동작이 어떻게 달라지는지 설명하시오.

> **모범 답안**
>
> Batch Normalization은 mini-batch의 feature 통계를 이용해 activation을 정규화한 뒤 learnable scale과 bias로 다시 조정하는 기법이다. 학습을 안정화하고 gradient flow를 개선하며, 보통 convolution layer와 ReLU 사이에 들어간다.
>
> Dropout은 학습 중 일부 neuron을 random하게 꺼서 모델이 특정 neuron 조합에 과도하게 의존하지 않도록 하는 regularization 기법이다. overfitting을 줄이고 일반화 성능을 높이는 데 도움이 된다.
>
> Training mode에서는 BatchNorm이 현재 batch 통계를 사용하고 Dropout이 random하게 neuron을 끈다. Evaluation mode에서는 BatchNorm이 학습 중 누적한 running mean/variance를 사용하고, Dropout은 꺼져 deterministic하게 동작한다.

채점 포인트

```
● BatchNorm = feature normalization + learnable scale/bias
● Dropout = neuron random masking으로 overfitting 완화
● train mode와 eval mode 차이
● eval에서는 deterministic해야 함
```

---

## 예측문제 8. Residual Connection의 필요성

Residual Connection이 깊은 신경망 학습에 도움이 되는 이유를 설명하시오.

> **모범 답안**
>
> Residual Connection은 layer block이 직접 `H(x)`를 학습하는 대신, 입력 `x`에 더할 변화량인 residual `F(x)=H(x)-x`를 학습하도록 만든다. 출력은 보통 `H(x)=F(x)+x` 형태가 된다.
>
> 이렇게 하면 block이 입력 feature를 완전히 새로 변환하기보다 기존 feature를 유지하면서 필요한 만큼만 refine할 수 있다. 또한 gradient가 shortcut 경로를 통해 더 잘 흐르므로 매우 깊은 네트워크에서도 vanishing gradient 문제를 완화하고 안정적으로 학습할 수 있다.
>
> 단, `F(x)`와 `x`를 더하려면 두 tensor의 shape이 맞아야 하므로, convolution block에서는 padding 등을 사용해 height/width와 channel 차원을 맞추는 것이 중요하다.

채점 포인트

```
● H(x)=F(x)+x 구조
● residual을 학습하는 것이 더 쉬움
● 깊은 네트워크에서 gradient flow 개선
● 더하기 위해 shape matching 필요
```

---

# 5. 최종 우선순위

| 우선순위 | 예측 주제 | 예상 세션 | 출제 가능성이 높은 이유 |
|---:|---|---|---|
| 1 | VAE loss: reconstruction + KL | `260417 오후`, `260420 오전/오후` | 기존 12문제에 생성형 모델이 없고 VAE 핵심 구조 |
| 2 | GAN Generator/Discriminator | `260421 오전/오후` | GAN 첫 핵심 개념이며 짧은 서술형으로 내기 좋음 |
| 3 | VAE vs Autoencoder / Reparameterization | `260420 오전/오후` | VAE가 일반 AE와 달라지는 핵심 |
| 4 | GAN mode collapse / 학습 불안정성 | `260421 오전/오후` | 강의에서 GAN 단점으로 강조됨 |
| 5 | VAE vs GAN 비교 | `260421 오후` | quality-diversity trade-off로 비교형 문제 가능 |
| 6 | BatchNorm과 Dropout | `260414 오후` | training/eval 차이를 묻기 좋음 |
| 7 | Residual Connection | `260414 오후`, `260413 오후` | 깊은 네트워크 학습과 gradient flow 연결 가능 |
| 8 | Self-Attention vs Cross-Attention / Masked Self-Attention | `260416 오전/오후`, `260417 오후` | Q11/Q12보다 한 단계 깊은 Transformer 문제 가능 |

숨겨진 4문제를 실제로 찍는다면 가장 유력한 조합은 다음이다.

```
1. VAE loss의 두 항
2. GAN의 Generator / Discriminator
3. GAN의 Mode Collapse 또는 VAE vs GAN 비교
4. BatchNorm/Dropout 또는 Residual Connection
```

핵심 결론: 기존 12문제가 기본 딥러닝 구조를 이미 커버했으므로, 숨겨진 4문제는 `260417`~`260421`의 생성형 모델(VAE/GAN)에서 가장 많이 나올 가능성이 높다.
