# AI Expert 자연어처리 시험 예측 문제 (260427 ~ 260504)

> 강의: 황승원 교수님 / 차혜준 강사 / LangAGI Lab
> 분석 대상: `260427-260504.md` 의 기출 8문제 + `260504(오전).md` 의 마지막 강의 (시험 직전 복습)
> 시험 범위: 260427 ~ 260504 (실제 강의가 진행된 8개 세션 + 마지막 복습)

---

# 분석 개요

## 1. 기출 8문제의 세션 분포

| 문제 | 주제 | 해당 세션 | 출제 형식 |
|------|------|-----------|-----------|
| Q1 | RNN 파라미터 행렬 차원 계산 ($W_{xh}, W_{hh}, W_{hy}, b_h$) | **260427 오전** (Word Embedding · RNN/LSTM) | 표 채우기 (계산형) |
| Q2 | RNN 5가지 구조 + Seq2Seq 구조적 장점 | **260427 오전 + 260428 오전** (Seq2Seq) | 다중 하위 문항 (예시·서술) |
| Q3 | Attention 메커니즘 — bottleneck + 디코딩 절차 | **260428 오전** (Attention) | 서술형 (개념 + 절차) |
| Q4 | BERT vs GPT (Encoder/Decoder 분류) + Decoding Strategy | **260428 오후 + 260429 오전** | 다중 하위 문항 (분류·비교) |
| Q5 | Transformer 인코더 블록 파라미터 수 계산 (1000개) | **260428 오후 + 260429 오전** (Transformer) | 표 채우기 (계산형) |
| Q6 | In-Context Learning + Zero-shot CoT prompt 작성 | **260429 오후** (ICL · CoT) | 다중 하위 문항 (서술 + 작문) |
| Q7 | Pairwise Preference + RLHF · DPO 활용 | **260430 오전** (RLHF) | 다중 하위 문항 (개념 + 적용) |
| Q8 | PPO 최적화 — Reward Model + KL Penalty | **260430 오전** (RLHF · PPO) | 서술형 (역할 설명) |

## 2. 기출 8문제의 출제 스타일

- **번호 1~10** 까지의 단일 큰 문제. **각 문제는 1~3개의 하위 문항** `(1) (2) (3)` 형태.
- **답안은 `정답` 섹션 형태로 1~3문장의 핵심 답안 + 채점 포인트 명시**.
- 형식 분포 (8문제 기준):
  - **계산형** 2문제 (Q1, Q5) — 파라미터 차원 / 개수.
  - **개념 비교·분류형** 3문제 (Q4, Q7, Q8) — 모델 분류 / 알고리즘 비교.
  - **서술형 (절차·이유 설명)** 2문제 (Q3, Q6) — 메커니즘 설명 + 작문.
  - **타입 매칭 + 예시** 1문제 (Q2) — 카테고리에 task 예시 매칭.
- **하나의 문제 내에서 (1) 개념 → (2) 적용/계산 → (3) 변형/비교** 의 구조가 반복적으로 나타남.
- 코드/수식 박스 ` ``` ` 활용 (Q6 prompt 예시, Q5 차원 표 등).
- "**채점 point:**" 표시로 정답의 핵심 포인트 명시.

## 3. 미커버 영역 — 출제 예상 범위

기출 8문제는 **Word Embedding, RNN, Seq2Seq, Attention, Transformer, BERT/GPT 분류, ICL/Zero-shot CoT, RLHF/DPO** 까지 다뤘다. 그러나 강의에서 **굵직하게 강조된 다음 영역은 아직 출제되지 않았음**:

- **260429 오전**: BERT의 두 학습 목표 (**MLM + NSP**) 메커니즘.
- **260429 오후**: **Few-shot CoT** (Zero-shot 외), **Self-Consistency / Majority Voting**, **Self-Refinement / Self-Reflection**, **Test-Time Scaling**, **Program-of-Thought (PoT)**.
- **260430 오전**: T5의 **Span Corruption + Denoising**, Closed-book QA, **Instruction Tuning vs Fine-Tuning** 차이, **Alignment vs Training** 개념.

## 4. 🔥 "숨은 2문제" — 강사 직접 힌트 분석

`260504(오전).md` 152번째 줄에서 강사가 **시험 문제 라인업을 직접 언급**:

> *"그 외에는 RLHF에서 PPO 리워드 모델, KL Divergence 패널티 같은 것들 설명하는 거 뭐 이런 거 냈던 것 같고,*
> ***체인업 섭(Chain-of-Thought), 체인업 섭 한 번 더 설명하는 거*** *뒤에 이제 낼 문제가 없어서 좀 샘플하고 서술형으로 풀어내시는 문제들을 제가 마무리한 것 같아요."*

이 발언을 해부하면:

1. PPO Reward Model + KL Penalty (= Q8) **이후의** 문제 흐름을 직접 언급.
2. **"체인업 섭"이 두 번 등장** → 9번, 10번 문제는 모두 **Chain-of-Thought 계열**.
3. "한 번 더 설명하는 거" → 첫 번째는 **CoT 자체**, 두 번째는 **CoT 변형/응용** 일 가능성이 매우 높음.
4. "샘플하고 서술형" → 객관식이 아닌 **순수 서술형** 으로 마무리 .

또한 `260504(오전).md` 170번 줄 부근에서 강사가 출제 수를 직접 확인:

> *"저희 내다라는 거는 10문제 내다라고 해서 방금 10문제 정도 냈고요. 실제로 다 드리겠죠? 10문제? … 아 10문제 4개 한 다음에 2문제 정도 걸러내나 봐요."*

→ **총 10문제 작성, 그 중 8문제는 이미 공개됨, 2문제는 미공개**.

### 🚨 결론
**숨은 2문제는 거의 확정적으로 Chain-of-Thought 계열의 서술형** 이다. 가능성이 높은 두 축은:

- **(A)** Few-shot CoT의 메커니즘 / Few-shot CoT vs Zero-shot CoT 비교
- **(B)** CoT의 확장 — Self-Consistency (Majority Voting) / Program-of-Thought (PoT) / Self-Refinement

또한 강의 내내 **Test-Time Scaling이 두 번** (260429 오후, 260504 오전) 강하게 강조되었으며, **Sequential vs Parallel** 카테고리화는 시험 출제 단골 프레임이다.

---

# 예상 문제지 (4문제)

## 문제

### Q9 [Chain-of-Thought (CoT) — 강사 직접 지목]

다음 prompt에 대해 답하시오.

```
Instruction: Tom has 5 apples. He buys 3 more apples and then gives 2 apples
to his friend. How many apples does Tom have now?
Answer:
```

**(1)** 위 문제를 **Few-shot CoT prompting** 으로 풀게 하려면, prompt를 어떻게 작성해야 하는지 **예시(rationale 포함)를 1개 이상 추가** 하여 작성하시오.

**(2)** Few-shot CoT가 단순한 standard prompting (답만 출력) 대비 **수학 reasoning task에서 성능을 향상시키는 이유** 를 설명하시오.

**(3)** Chain-of-Thought 능력은 **모델 사이즈가 일정 임계점을 넘어서야** 발현되는 emergent ability로 알려져 있다. 그 이유를 간단히 설명하시오.

> **답안**

---

### Q10 [Test-Time Scaling — Self-Consistency / PoT — 강사 직접 지목]

LLM의 추론 성능을 향상시키기 위한 다양한 **Test-Time Scaling 기법** 이 제안되었다.

**(1)** **Self-Consistency (Majority Voting)** 가 무엇인지 설명하고, 이것이 Chain-of-Thought (CoT) 와 어떤 관계로 결합되는지 서술하시오.

**(2)** Self-Consistency는 Test-Time Scaling 의 두 카테고리 (**Sequential** vs **Parallel**) 중 어느 쪽에 해당하는가? 그리고 **Sequential** 쪽 대표 기법 1가지를 함께 제시하시오.

**(3)** **Program-of-Thought (PoT)** 가 일반 Chain-of-Thought 와 다른 점을 한 가지 서술하고, PoT가 등장하게 된 **LLM의 한계 (왜 LLM이 산술에 약한가)** 를 토큰화 관점에서 설명하시오.

> **답안**

---

### Q11 [BERT의 사전학습 목표 — 미커버 영역]

BERT는 두 가지 사전학습 목표(pre-training objective)를 동시에 학습하였다.

**(1)** **Masked Language Modeling (MLM)** 와 **Next Sentence Prediction (NSP)** 이 각각 무엇인지 설명하시오.

**(2)** GPT의 학습 목표 (**Causal / Autoregressive Language Modeling**) 와 비교하여, **BERT가 양방향 (bidirectional) 표현을 배울 수 있는 이유**를 설명하시오.

> **답안**

---

### Q12 [Self-Refinement (Self-Reflection) — Test-Time Scaling Sequential]

LLM이 한 번에 답을 생성하는 대신 **자기 출력을 스스로 평가하고 개선** 하는 방식이 최근 에이전트의 핵심 기술로 사용되고 있다.

**(1)** **Self-Refinement / Self-Reflection** 절차를 `Generate → Critique → Refine` 의 흐름으로 설명하시오.

**(2)** Self-Refinement가 **하나의 동일한 모델만 사용** 함에도 효과를 내는 이유를 "Initial Generation Task vs Critique Task vs Refinement Task" 능력 차이 관점에서 서술하시오.

> **답안**

---

# 예상 문제지 답안

### Q9 [Chain-of-Thought (CoT)]

**(1)**
```
Instruction: Anna has 4 oranges. She buys 5 more, then eats 1.
How many oranges does Anna have now?
Answer: Anna started with 4. She bought 5, so 4 + 5 = 9.
She ate 1, so 9 − 1 = 8. The answer is 8.

Instruction: Tom has 5 apples. He buys 3 more apples and then gives 2 apples
to his friend. How many apples does Tom have now?
Answer:
```
> 핵심 채점 포인트: **예시(Q-A 페어)에 reasoning rationale (계산 과정)이 포함되어 있고**, 그 다음 target 문제를 자연스럽게 이어서 배치할 것.

**(2)**
> **모범 답안**
>
> Standard prompting은 모델이 단번에 정답 토큰을 생성해야 하므로, 한 번의 forward 계산만으로는 정보 밀도가 낮은 토큰들이 답을 결정짓게 된다. CoT는 모델이 중간 추론 단계를 토큰으로 풀어내도록 유도하여 **더 많은 토큰을 소비해 확률 분포를 sharper 하게 만든다**. 즉 어려운 reasoning일수록 추가적인 계산(추가 토큰)이 필요하다는 원리에 기반하며, 그 결과 GSM8K 같은 reasoning 벤치마크에서 큰 폭의 성능 향상이 발생한다.

**(3)**
> **모범 답안**
>
> CoT는 small model에서는 거의 효과가 없고, 일정 사이즈 이상의 large language model에서만 성능 갭이 갑자기 벌어지는 **Emergent Ability** 이다. 이는 추론 과정 자체를 자연어로 매끄럽게 생성할 수 있는 언어 능력 + reasoning 패턴 인식이 큰 모델에서야 동시에 안정적으로 발현되기 때문이며, 작은 모델은 rationale을 생성하더라도 일관된 reasoning이 깨져 오히려 성능이 떨어질 수 있다.

---

### Q10 [Test-Time Scaling — Self-Consistency / PoT]

**(1)**
> **모범 답안**
>
> Self-Consistency 는 동일한 prompt에 대해 **CoT를 여러 번 (N번) 생성**한 뒤, 각 reasoning chain이 도출한 최종 답들 중 **가장 많이 나온 답을 다수결(majority voting)로 선택** 하는 기법이다. CoT는 추론 경로를 1개만 만들지만 Self-Consistency는 여러 개를 만들어 답의 안정성을 높이므로, **CoT의 확장형(CoT-SC)** 으로 결합되어 사용된다.

**(2)**
> **모범 답안**
>
> Self-Consistency 는 여러 응답을 동시에 생성한 뒤 선택하는 구조이므로 **Parallel** 카테고리에 해당한다. **Sequential** 쪽 대표 기법으로는 **Thinking Mode (Long CoT, o1/o3/DeepSeek-R1 류)** 또는 **Self-Refinement** 가 있다.
>
> *(채점 포인트: Parallel 명시 + Sequential 예시 1개)*

**(3)**
> **모범 답안**
>
> Program-of-Thought (PoT) 는 자연어 rationale 대신 **모델이 Python 같은 코드를 생성**하고, 그 코드를 외부 인터프리터로 실행시켜 답을 얻는다는 점에서 일반 CoT와 다르다. LLM이 산술에 약한 이유는 **토큰화 방식 때문** 인데, "1723" 같은 숫자가 사람에게는 통째로 한 숫자이지만 토크나이저는 "17" + "23" 처럼 쪼개고 띄어쓰기에 따라 같은 숫자가 다른 토큰이 되는 등, **인간이 인식하는 숫자와 모델이 인식하는 토큰이 일치하지 않기 때문**이다. PoT는 이 문제를 외부 컴퓨테이션으로 우회한다.

---

### Q11 [BERT의 사전학습 목표]

**(1)**
> **모범 답안**
>
> - **MLM (Masked Language Modeling)**: 입력 문장에서 일부 토큰(약 15%)을 `[MASK]`로 가리고, 모델이 가려진 자리의 원래 토큰이 무엇인지 예측하도록 학습하는 방법. 양쪽 문맥을 모두 보고 빈칸을 채우는 방식.
> - **NSP (Next Sentence Prediction)**: 두 문장 A, B를 입력으로 주고, B가 실제로 A의 다음 문장인지 (`IsNext`) 아니면 무관한 문장인지 (`NotNext`)를 이진 분류로 예측. 문장 간 관계를 학습시키기 위한 보조 task.

**(2)**
> **모범 답안**
>
> GPT는 **left-to-right autoregressive LM** 이므로 t번째 토큰을 예측할 때 t보다 이전의 토큰만 볼 수 있는 단방향 모델이다. 반면 BERT는 MLM으로 빈칸을 맞추는 task이기 때문에, 빈칸의 **왼쪽 context와 오른쪽 context를 동시에** 인코더가 참조할 수 있어 **bidirectional 표현** 을 학습한다. 이로 인해 NLU(텍스트 이해) 계열 task에서 GPT보다 강한 표현력을 가지게 된다.

---

### Q12 [Self-Refinement / Self-Reflection]

**(1)**
> **모범 답안**
>
> Self-Refinement는 한 번의 generation으로 답을 확정하지 않고 다음 흐름을 거친다.
> 1. **Generate**: 모델이 입력 prompt에 대해 초기 응답(Initial Output)을 생성.
> 2. **Critique**: 동일 모델이 자신의 출력을 입력으로 받아 잘못된 점·부족한 점을 평가하는 feedback을 생성.
> 3. **Refine**: 그 feedback을 다시 입력으로 받아 출력을 수정한 Refined Output을 생성.
> 만족스러울 때까지 (또는 정해진 횟수만큼) 이 사이클을 반복한다.

**(2)**
> **모범 답안**
>
> 같은 모델이라도 task별로 발현되는 능력치가 다르기 때문이다. 즉 **Initial Generation Task** (정답을 처음 생성), **Critique Task** (틀린 곳 찾기), **Refinement Task** (피드백을 받고 수정) 은 각각 별개의 sub-skill 이며, 모델은 자신이 한 번에 풀기 어려운 문제도 "**자신의 출력을 비판적으로 평가**" 하는 과제로 바꾸면 더 잘 풀 수 있다. 이 원리 때문에 Claude Code, Cursor 같은 에이전트가 에러 메시지를 받아 스스로 수정하는 동작이 가능하며, "Multi-Agent" 도 결국 같은 모델에 다른 system prompt를 부여하여 역할을 분리한 self-loop의 확장이다.

---

# 출제 확률 근거 요약

| 예측 문제 | 출제 확률 근거 |
|-----------|----------------|
| **Q9 Chain-of-Thought** | 강사가 시험 라인업을 언급하며 **"체인업 섭"을 직접 호명**. Q6은 Zero-shot CoT prompt 작성만 다뤘으므로 **Few-shot CoT의 rationale 작성 + 메커니즘 설명** 이 별도 문제로 분리될 가능성이 매우 높음. |
| **Q10 Self-Consistency / PoT** | 강사가 **"체인업 섭 한 번 더 설명"** 이라고 두 번 언급. 260429 오후 + 260504 오전 모두에서 Test-Time Scaling (Sequential / Parallel) 프레임이 강조됨. PoT는 "토큰화 때문에 산술 약함" 이라는 직관적 hook이 있어 서술형으로 매우 적합. |
| **Q11 BERT MLM + NSP** | 260429 오전이 BERT-heavy 였으나 기출 Q4는 BERT를 "Encoder-only" 라는 분류만 다룸. **MLM/NSP 메커니즘 자체** 는 현재 문제집에서 빠진 핵심 영역. 강사가 "버트, GPT, T5" 가 모두 시험 가능 영역이라고 직접 언급함 (260504 오전 line 89-105). |
| **Q12 Self-Refinement** | Test-Time Scaling Sequential 의 대표 기법으로 강사가 **"클로드 코드/커서 같은 에이전트의 핵심 원리"** 라고 설명. 260504 오전 마지막 강의에서 **Self-Verification / Self-Reflection** 이 다시 강조됨. 서술형 + 절차 설명에 매우 적합. |

> **단, Q11과 Q12 중 한 문제만 출제되고 다른 한 문제는 출제되지 않을 수도 있음**. 강사 힌트에 의하면 숨은 2문제가 모두 CoT 계열일 가능성이 더 높지만, 위 4문제는 모두 **출제되지 않더라도 학습 가치가 매우 높은** 핵심 개념이므로 함께 준비해두는 것을 권장.

---

# 한 페이지 핵심 정리 — 시험 직전 체크리스트

1. **CoT의 동작 원리**: 토큰을 더 소비할수록 확률이 sharper 해진다 (Token Information Density).
2. **Few-shot CoT vs Zero-shot CoT**: 예시 vs `Let's think step by step`.
3. **Emergent Ability**: 일정 모델 사이즈 이상에서 갑자기 발현.
4. **Test-Time Scaling 두 카테고리**:
   - **Sequential**: Thinking Mode, Self-Refinement
   - **Parallel**: Self-Consistency (Majority Voting), Best-of-N
5. **Self-Consistency = CoT 여러 번 + 다수결** (= 옛 명칭 CoT-SC).
6. **PoT 의 동기**: 토큰화 한계 → 코드로 우회.
7. **Self-Refinement = Generate → Critique → Refine**: 같은 모델, task별 능력 차이.
8. **BERT = MLM + NSP**, GPT = Causal LM. BERT는 양방향, GPT는 단방향.
9. **Test-Time Scaling = Agent 시대의 핵심 컴퓨팅 패러다임** (강사 메시지).
10. **모든 추론 기법은 본질적으로 모델과 대화하는 "프로토콜의 발견"** 이다 (강사 표현).
